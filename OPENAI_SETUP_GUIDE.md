# 🚀 دليل إعداد OpenAI API للتحويل الصوتي

## ⚡ البدء السريع (Quick Start)

### **1. احصل على مفتاح OpenAI API**

```
1. اذهب إلى: https://platform.openai.com/signup
2. أنشئ حساب أو سجل دخول
3. أضف رصيد ($5 كحد أدنى): https://platform.openai.com/account/billing
4. أنشئ مفتاح API: https://platform.openai.com/api-keys
5. انسخ المفتاح (يبدأ بـ sk-...)
```

### **2. أضف المفتاح في المشروع**

افتح ملف `.env.local` وعدّل السطر التالي:

```bash
# قبل التعديل:
OPENAI_API_KEY=your-openai-api-key-here

# بعد التعديل (مثال):
OPENAI_API_KEY=sk-proj-abc123xyz789...
```

### **3. أعد تشغيل السيرفر**

```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغله من جديد:
npm run dev
```

### **4. جرّب الميزة!**

1. اذهب إلى صفحة إضافة طلب: `/dashboard/add-order`
2. سجل ملاحظة صوتية
3. اضغط على زر "تحويل إلى نص" 📝
4. انتظر بضع ثوانٍ... ✨
5. سيظهر النص المحول! 🎉

---

## 💰 التكلفة

### **Whisper API (تحويل الصوت إلى نص):**
- **$0.006 لكل دقيقة**
- مثال: 100 تسجيل × 30 ثانية = 50 دقيقة = **$0.30**

### **مثال واقعي:**
```
رصيد $5 = حوالي 833 دقيقة من التحويل
833 دقيقة ÷ 0.5 دقيقة لكل تسجيل = 1,666 تسجيل!
```

**$5 تكفي لأشهر من الاستخدام!** 🎉

---

## 🔒 الأمان

### **✅ ما يجب فعله:**
1. احفظ مفتاح API في `.env.local` فقط
2. لا تشارك ملف `.env.local` مع أحد
3. لا ترفع ملف `.env.local` إلى GitHub
4. استخدم `.gitignore` للتأكد من عدم رفع الملف

### **❌ ما يجب تجنبه:**
1. لا تضع المفتاح في الكود مباشرة
2. لا تشارك المفتاح في الرسائل أو البريد الإلكتروني
3. لا تنشر المفتاح على الإنترنت

---

## 🐛 حل المشاكل الشائعة

### **المشكلة 1: "OpenAI API key not configured"**
**الحل:**
- تأكد من إضافة `OPENAI_API_KEY` في `.env.local`
- تأكد من إعادة تشغيل السيرفر بعد التعديل

### **المشكلة 2: "Incorrect API key provided"**
**الحل:**
- تأكد من نسخ المفتاح بالكامل (يبدأ بـ `sk-`)
- تأكد من عدم وجود مسافات قبل أو بعد المفتاح

### **المشكلة 3: "You exceeded your current quota"**
**الحل:**
- أضف رصيد في حسابك: https://platform.openai.com/account/billing
- تحقق من استهلاكك: https://platform.openai.com/usage

### **المشكلة 4: التحويل بطيء جداً**
**الحل:**
- هذا طبيعي! التحويل يستغرق 2-5 ثوانٍ حسب طول التسجيل
- تأكد من سرعة الإنترنت لديك

---

## 📊 مراقبة الاستخدام

### **كيف تتحقق من استهلاكك:**

1. اذهب إلى: https://platform.openai.com/usage
2. ستجد تقرير مفصل عن:
   - عدد الطلبات (Requests)
   - عدد الدقائق المستخدمة (Minutes)
   - التكلفة الإجمالية (Total Cost)

### **نصائح لتوفير التكلفة:**

1. ✅ استخدم التحويل فقط عند الحاجة
2. ✅ راجع النص المحول قبل الترجمة
3. ✅ احذف التسجيلات غير المهمة
4. ✅ راقب استهلاكك شهرياً

---

## 🎯 الخلاصة

### **ما تم إضافته:**
- ✅ تحويل الصوت إلى نص باستخدام OpenAI Whisper
- ✅ دقة عالية جداً (99%+)
- ✅ يعمل على جميع المتصفحات
- ✅ تكلفة منخفضة جداً

### **ما تحتاجه للبدء:**
1. ✅ مفتاح OpenAI API
2. ✅ رصيد $5 (يكفي لأشهر)
3. ✅ إضافة المفتاح في `.env.local`
4. ✅ إعادة تشغيل السيرفر

**جاهز للاستخدام!** 🚀

---

## 📞 الدعم الفني

إذا واجهت أي مشكلة:
1. راجع قسم "حل المشاكل الشائعة" أعلاه
2. تحقق من ملف `VOICE_TRANSCRIPTION_GUIDE.md` للتفاصيل الكاملة
3. تواصل مع فريق الدعم

**بالتوفيق!** 🎉

