# دليل البدء السريع - نظام أنواع العمال
# Quick Start Guide - Worker Types System

## ⚡ خطوات التفعيل السريعة

### الخطوة 1: تطبيق Migration في قاعدة البيانات ✅

1. افتح **Supabase Dashboard**
2. اذهب إلى **SQL Editor**
3. انسخ محتوى ملف `supabase/migrations/add_worker_types.sql`
4. الصق المحتوى في SQL Editor
5. اضغط **Run** لتنفيذ الأوامر

**أو** يمكنك تنفيذ الأمر التالي مباشرة:

```sql
ALTER TABLE workers 
ADD COLUMN IF NOT EXISTS worker_type TEXT DEFAULT 'tailor' 
CHECK (worker_type IN ('tailor', 'fabric_store_manager', 'accountant', 'general_manager'));

UPDATE workers 
SET worker_type = 'tailor' 
WHERE worker_type IS NULL;
```

---

### الخطوة 2: إعادة تشغيل التطبيق 🔄

```bash
npm run dev
```

---

### الخطوة 3: اختبار النظام 🧪

#### أ. إضافة عامل جديد:

1. سجل دخول كـ **Admin**
2. اذهب إلى `/dashboard/workers`
3. اضغط **"إضافة عامل جديد"**
4. املأ البيانات:
   - الاسم الكامل: `أحمد محمد`
   - البريد الإلكتروني: `fabric.manager@test.com`
   - كلمة المرور: `123456`
   - رقم الهاتف: `0501234567`
   - التخصص: `إدارة الأقمشة`
   - **نوع العامل:** اختر **"مدير متجر الأقمشة"**
5. احفظ

#### ب. اختبار تسجيل الدخول:

1. سجل خروج من حساب Admin
2. سجل دخول بحساب العامل الجديد:
   - البريد: `fabric.manager@test.com`
   - كلمة المرور: `123456`
3. **النتيجة المتوقعة:**
   - يتم توجيهك إلى `/dashboard/fabric-manager`
   - تظهر لك لوحة تحكم مخصصة لمدير الأقمشة
   - يمكنك الوصول فقط إلى صفحة الأقمشة

#### ج. اختبار الصلاحيات:

1. وأنت مسجل دخول كمدير أقمشة، حاول الوصول إلى:
   - `/dashboard/workers` ❌ (سيتم منعك)
   - `/dashboard/accounting` ❌ (سيتم منعك)
   - `/dashboard/fabrics` ✅ (مسموح)

---

## 📊 أمثلة على إنشاء العمال

### مثال 1: مدير عام
```
الاسم: محمد أحمد
البريد: general.manager@test.com
كلمة المرور: 123456
الهاتف: 0501111111
التخصص: إدارة عامة
نوع العامل: مدير عام
```
**النتيجة:** وصول كامل لجميع الصفحات

---

### مثال 2: محاسب
```
الاسم: فاطمة علي
البريد: accountant@test.com
كلمة المرور: 123456
الهاتف: 0502222222
التخصص: محاسبة
نوع العامل: محاسب
```
**النتيجة:** وصول فقط للنظام المحاسبي

---

### مثال 3: خياطة
```
الاسم: نورة سعيد
البريد: tailor@test.com
كلمة المرور: 123456
الهاتف: 0503333333
التخصص: فساتين زفاف
نوع العامل: خياط/خياطة
```
**النتيجة:** وصول فقط لطلبات التفصيل

---

## 🎯 الصفحات الجديدة

| الصفحة | الرابط | من يمكنه الوصول |
|--------|--------|-----------------|
| لوحة مدير الأقمشة | `/dashboard/fabric-manager` | مدير أقمشة + Admin |
| لوحة المحاسب | `/dashboard/accountant` | محاسب + Admin |
| إدارة الأقمشة | `/dashboard/fabrics` | مدير أقمشة + Admin |
| النظام المحاسبي | `/dashboard/accounting` | محاسب + Admin |

---

## ✅ التحقق من نجاح التطبيق

### 1. تحقق من قاعدة البيانات:
```sql
-- في Supabase SQL Editor
SELECT 
  w.id,
  u.full_name,
  u.email,
  w.specialty,
  w.worker_type
FROM workers w
JOIN users u ON w.user_id = u.id
ORDER BY w.created_at DESC;
```

### 2. تحقق من الواجهة:
- افتح `/dashboard/workers`
- يجب أن ترى حقل "نوع العامل" في نموذج الإضافة
- يجب أن ترى نوع العامل معروضاً في بطاقة كل عامل

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: خطأ عند إضافة عامل
**الحل:** تأكد من تطبيق migration في قاعدة البيانات

### المشكلة 2: لا يظهر حقل "نوع العامل"
**الحل:** أعد تشغيل التطبيق (`npm run dev`)

### المشكلة 3: العامل لا يتم توجيهه للصفحة الصحيحة
**الحل:** تأكد من أن `worker_type` محفوظ بشكل صحيح في قاعدة البيانات

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع ملف `WORKER_TYPES_GUIDE.md` للتفاصيل الكاملة
2. تحقق من console في المتصفح للأخطاء
3. تحقق من logs في Supabase Dashboard

---

**تم! 🎉 النظام جاهز للاستخدام**

