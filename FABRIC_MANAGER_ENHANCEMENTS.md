# 🎨 تحسينات صفحة مدير الأقمشة
# Fabric Manager Dashboard Enhancements

---

## ✅ **التعديلات المنفذة:**

### **1️⃣ إضافة زر تسجيل الخروج** ✅

**الموقع:** Header الصفحة (الزاوية العلوية اليسرى)

**الميزات:**
- ✅ زر واضح ومتناسق مع تصميم الصفحة
- ✅ أيقونة `LogOut` من lucide-react
- ✅ تصميم responsive (يخفي النص في الشاشات الصغيرة)
- ✅ ألوان حمراء للدلالة على الخروج
- ✅ تأثيرات hover جميلة

**الوظيفة:**
```typescript
const handleSignOut = async () => {
  await signOut()  // تسجيل الخروج من Supabase
  router.push('/login')  // إعادة التوجيه لصفحة تسجيل الدخول
}
```

**ما يحدث عند الضغط:**
1. ✅ تسجيل خروج المستخدم من Supabase Auth
2. ✅ مسح بيانات الجلسة من localStorage
3. ✅ مسح بيانات المستخدم من Zustand store
4. ✅ إعادة التوجيه إلى `/login`

---

### **2️⃣ إضافة قسم إدارة موظفي متجر الأقمشة** ✅

**الموقع:** قسم جديد في صفحة `/dashboard/fabric-manager`

**الميزات:**
- ✅ بطاقة قابلة للنقر لإظهار/إخفاء قسم الموظفين
- ✅ عرض فقط الموظفين الذين لديهم `worker_type = 'fabric_store_manager'`
- ✅ تصميم متجاوب (responsive) - grid يتحول لعمود واحد في الشاشات الصغيرة
- ✅ تأثيرات حركية (animations) عند الإظهار/الإخفاء

**الصلاحيات:**
- ✅ **عرض** قائمة الموظفين (الاسم، البريد، الهاتف، الحالة، تاريخ الانضمام)
- ✅ **تعديل** بيانات الموظفين (الاسم، الهاتف، الحالة النشطة/غير نشطة)
- ❌ **لا يمكن** إضافة موظفين جدد
- ❌ **لا يمكن** حذف الموظفين
- ❌ **لا يمكن** تعديل البريد الإلكتروني

---

## 📁 **الملفات المنشأة/المعدلة:**

### **1. Component جديد: `FabricStoreWorkersManager.tsx`**

**الموقع:** `src/components/FabricStoreWorkersManager.tsx`

**الوظائف:**
- ✅ تحميل جميع العمال من Supabase
- ✅ فلترة الموظفين حسب `worker_type = 'fabric_store_manager'`
- ✅ عرض الموظفين في بطاقات جميلة
- ✅ نافذة منبثقة (modal) لتعديل بيانات الموظف
- ✅ رسائل نجاح/خطأ عند التحديث

**الحقول القابلة للتعديل:**
- ✅ الاسم الكامل (`full_name`)
- ✅ رقم الهاتف (`phone`)
- ✅ حالة الحساب (`is_active`) - نشط/غير نشط

**الحقول للقراءة فقط:**
- 📧 البريد الإلكتروني (`email`)
- 📅 تاريخ الانضمام (`created_at`)
- 💼 نوع العامل (`worker_type`)

---

### **2. تعديل صفحة: `fabric-manager/page.tsx`**

**الموقع:** `src/app/dashboard/fabric-manager/page.tsx`

**التعديلات:**
1. ✅ إضافة import لـ `LogOut` و `Users` icons
2. ✅ إضافة import لـ `FabricStoreWorkersManager` component
3. ✅ إضافة state لإظهار/إخفاء قسم الموظفين
4. ✅ إضافة دالة `handleSignOut()`
5. ✅ إضافة زر تسجيل الخروج في Header
6. ✅ تحويل Info Cards من 3 إلى 2 بطاقات
7. ✅ إضافة بطاقة "إدارة الموظفين" قابلة للنقر
8. ✅ إضافة قسم منفصل لعرض الموظفين (يظهر/يختفي عند النقر)

---

## 🎨 **التصميم:**

### **زر تسجيل الخروج:**
```tsx
<button
  onClick={handleSignOut}
  className="flex items-center gap-2 px-4 sm:px-6 py-2 sm:py-3 
             bg-red-50 hover:bg-red-100 
             text-red-600 hover:text-red-700 
             rounded-full transition-all duration-300 
             font-medium border border-red-200 hover:border-red-300 
             shadow-sm hover:shadow-md"
>
  <LogOut className="w-4 h-4 sm:w-5 sm:h-5" />
  <span className="hidden sm:inline">تسجيل الخروج</span>
</button>
```

**الألوان:**
- 🔴 خلفية: `bg-red-50` → `bg-red-100` (hover)
- 🔴 نص: `text-red-600` → `text-red-700` (hover)
- 🔴 حدود: `border-red-200` → `border-red-300` (hover)

### **بطاقة إدارة الموظفين:**
```tsx
<button className="bg-white/80 backdrop-blur-sm rounded-2xl p-6 
                   shadow-lg border border-pink-100/50 
                   hover:shadow-xl transition-all duration-300">
  <div className="w-12 h-12 bg-gradient-to-br from-indigo-400 to-purple-500 
                  rounded-xl flex items-center justify-center 
                  shadow-md group-hover:scale-110 transition-transform">
    <Users className="w-6 h-6 text-white" />
  </div>
  <h3>إدارة الموظفين</h3>
  <p>عرض وتعديل بيانات موظفي متجر الأقمشة</p>
</button>
```

**الألوان:**
- 🟣 أيقونة: gradient من `indigo-400` إلى `purple-500`
- 🌸 حدود: `border-pink-100/50`
- ⚪ خلفية: `bg-white/80` مع `backdrop-blur-sm`

### **بطاقات الموظفين:**
```tsx
<div className="bg-white/80 backdrop-blur-sm rounded-2xl p-6 
                border border-pink-100 hover:shadow-lg">
  {/* Header مع أيقونة وحالة */}
  <div className="flex items-start justify-between">
    <div className="w-12 h-12 bg-gradient-to-br from-pink-400 to-rose-400 
                    rounded-full flex items-center justify-center">
      <User className="w-6 h-6 text-white" />
    </div>
    <div className="px-3 py-1 rounded-full text-xs 
                    bg-green-100 text-green-700">
      نشط
    </div>
  </div>
  
  {/* معلومات الموظف */}
  <div className="space-y-2">
    <div className="flex items-center gap-2">
      <Mail className="w-4 h-4 text-pink-500" />
      <span>email@example.com</span>
    </div>
    {/* ... */}
  </div>
</div>
```

---

## 🔧 **الوظائف التقنية:**

### **تحميل الموظفين:**
```typescript
useEffect(() => {
  loadWorkers()  // تحميل من Supabase
}, [loadWorkers])

useEffect(() => {
  // فلترة موظفي متجر الأقمشة فقط
  const fabricWorkers = workers.filter(
    w => w.worker_type === 'fabric_store_manager'
  )
  setFabricStoreWorkers(fabricWorkers)
}, [workers])
```

### **تعديل موظف:**
```typescript
const handleUpdateWorker = async () => {
  const updates = {
    full_name: editingWorker.full_name,
    phone: editingWorker.phone,
    is_active: editingWorker.is_active
  }

  const result = await updateWorker(editingWorker.id, updates)

  if (result.success) {
    setMessage({ type: 'success', text: 'تم تحديث بيانات الموظف بنجاح' })
    loadWorkers()  // إعادة تحميل القائمة
  }
}
```

---

## 📊 **الصلاحيات والأمان:**

### **من يمكنه الوصول:**
| المستخدم | الوصول | الصلاحيات |
|---------|--------|-----------|
| **Admin** | ✅ | عرض + تعديل جميع الموظفين |
| **مدير الأقمشة** | ✅ | عرض + تعديل موظفي متجر الأقمشة فقط |
| **الخياط** | ❌ | لا يوجد وصول |
| **المحاسب** | ❌ | لا يوجد وصول |
| **العميل** | ❌ | لا يوجد وصول |

### **RLS Policies المطلوبة:**

**ملاحظة:** قد تحتاج إلى تحديث RLS policies في Supabase للسماح لمدير الأقمشة بقراءة وتعديل بيانات الموظفين.

```sql
-- السماح لمدير الأقمشة بقراءة موظفي متجر الأقمشة
CREATE POLICY "Fabric managers can view fabric store workers"
ON public.workers
FOR SELECT
TO authenticated
USING (
  worker_type = 'fabric_store_manager'
  AND EXISTS (
    SELECT 1 FROM public.users u
    LEFT JOIN public.workers w ON w.user_id = u.id
    WHERE u.id = auth.uid()
    AND (
      u.role = 'admin'
      OR (u.role = 'worker' AND w.worker_type = 'fabric_store_manager')
    )
  )
);

-- السماح لمدير الأقمشة بتعديل موظفي متجر الأقمشة
CREATE POLICY "Fabric managers can update fabric store workers"
ON public.workers
FOR UPDATE
TO authenticated
USING (
  worker_type = 'fabric_store_manager'
  AND EXISTS (
    SELECT 1 FROM public.users u
    LEFT JOIN public.workers w ON w.user_id = u.id
    WHERE u.id = auth.uid()
    AND (
      u.role = 'admin'
      OR (u.role = 'worker' AND w.worker_type = 'fabric_store_manager')
    )
  )
);
```

---

## 🧪 **اختبار الميزات:**

### **1. اختبار زر تسجيل الخروج:**

**الخطوات:**
1. ✅ افتح صفحة `/dashboard/fabric-manager`
2. ✅ تحقق من ظهور زر "تسجيل الخروج" في الزاوية العلوية اليسرى
3. ✅ اضغط على الزر
4. ✅ تحقق من إعادة التوجيه إلى `/login`
5. ✅ حاول الوصول إلى `/dashboard/fabric-manager` مرة أخرى
6. ✅ يجب أن يتم إعادة توجيهك إلى `/login` (لأنك غير مسجل دخول)

**النتيجة المتوقعة:**
- ✅ تسجيل الخروج بنجاح
- ✅ مسح بيانات الجلسة
- ✅ إعادة التوجيه إلى صفحة تسجيل الدخول
- ✅ عدم القدرة على الوصول للصفحات المحمية

---

### **2. اختبار قسم إدارة الموظفين:**

**الخطوات:**
1. ✅ افتح صفحة `/dashboard/fabric-manager`
2. ✅ ابحث عن بطاقة "إدارة الموظفين"
3. ✅ اضغط على البطاقة
4. ✅ تحقق من ظهور قسم الموظفين بتأثير حركي
5. ✅ تحقق من عرض موظفي متجر الأقمشة فقط
6. ✅ اضغط على "تعديل البيانات" لأحد الموظفين
7. ✅ عدّل الاسم أو رقم الهاتف أو الحالة
8. ✅ اضغط "حفظ التعديلات"
9. ✅ تحقق من ظهور رسالة نجاح
10. ✅ تحقق من تحديث البيانات في القائمة

**النتيجة المتوقعة:**
- ✅ عرض موظفي متجر الأقمشة فقط
- ✅ إمكانية تعديل الاسم والهاتف والحالة
- ✅ عدم إمكانية تعديل البريد الإلكتروني
- ✅ رسائل نجاح/خطأ واضحة
- ✅ تحديث البيانات فوراً

---

### **3. اختبار Responsive Design:**

**الخطوات:**
1. ✅ افتح الصفحة على شاشة كبيرة (Desktop)
   - تحقق من ظهور نص "تسجيل الخروج" بجانب الأيقونة
   - تحقق من عرض بطاقات الموظفين في عمودين
2. ✅ افتح الصفحة على شاشة متوسطة (Tablet)
   - تحقق من تكيف التصميم
3. ✅ افتح الصفحة على شاشة صغيرة (Mobile)
   - تحقق من إخفاء نص "تسجيل الخروج" (الأيقونة فقط)
   - تحقق من عرض بطاقات الموظفين في عمود واحد

**النتيجة المتوقعة:**
- ✅ تصميم متجاوب على جميع الشاشات
- ✅ لا يوجد overflow أو تداخل
- ✅ سهولة الاستخدام على الهاتف

---

## ⚠️ **ملاحظات مهمة:**

### **1. RLS Policies:**

**قد تحتاج إلى تطبيق RLS policies** إذا واجهت أخطاء عند محاولة عرض أو تعديل الموظفين:

```
Error: new row violates row-level security policy
```

**الحل:**
1. افتح Supabase Dashboard
2. اذهب إلى SQL Editor
3. انسخ والصق SQL من القسم "RLS Policies المطلوبة" أعلاه
4. شغّل الـ SQL
5. أعد تحميل الصفحة

---

### **2. تعديل البريد الإلكتروني:**

**لماذا لا يمكن تعديل البريد الإلكتروني؟**

البريد الإلكتروني مرتبط بـ Supabase Auth، وتعديله يتطلب:
- استخدام Supabase Admin API
- إعادة تأكيد البريد الجديد
- تحديث في جدول `auth.users` (وليس `public.users` فقط)

**إذا أردت إضافة هذه الميزة:**
1. أنشئ API route في `/api/admin/update-user-email`
2. استخدم Supabase Admin Client
3. أضف validation للبريد الجديد
4. أرسل email تأكيد للمستخدم

---

### **3. الأداء:**

**تحسينات مستقبلية:**
- ✅ إضافة pagination للموظفين (إذا كان العدد كبير)
- ✅ إضافة بحث/فلترة للموظفين
- ✅ إضافة sorting (ترتيب حسب الاسم، التاريخ، الحالة)
- ✅ إضافة export للبيانات (CSV/Excel)

---

## 🎉 **الخلاصة:**

### **ما تم إنجازه:**
1. ✅ إضافة زر تسجيل الخروج في صفحة مدير الأقمشة
2. ✅ إضافة قسم إدارة موظفي متجر الأقمشة
3. ✅ إمكانية عرض وتعديل بيانات الموظفين
4. ✅ تصميم responsive ومتناسق مع الصفحة
5. ✅ تأثيرات حركية جميلة
6. ✅ رسائل نجاح/خطأ واضحة

### **الملفات المنشأة:**
1. ✅ `src/components/FabricStoreWorkersManager.tsx` - Component إدارة الموظفين
2. ✅ `FABRIC_MANAGER_ENHANCEMENTS.md` - دليل شامل للتحسينات

### **الملفات المعدلة:**
1. ✅ `src/app/dashboard/fabric-manager/page.tsx` - إضافة زر الخروج وقسم الموظفين

### **الخطوات التالية:**
1. ⏳ اختبار زر تسجيل الخروج
2. ⏳ اختبار عرض وتعديل الموظفين
3. ⏳ تطبيق RLS policies إذا لزم الأمر
4. ⏳ اختبار Responsive design على أجهزة مختلفة

---

## 📞 **الدعم:**

إذا واجهت أي مشاكل:
1. تحقق من Console في المتصفح (F12)
2. تحقق من RLS policies في Supabase
3. تحقق من أن المستخدم لديه `worker_type = 'fabric_store_manager'`
4. تحقق من أن Supabase متصل بشكل صحيح

---

**تم بنجاح! 🎉**

جميع التعديلات المطلوبة تم تنفيذها بنجاح. الصفحة الآن تحتوي على:
- ✅ زر تسجيل خروج واضح وجميل
- ✅ قسم كامل لإدارة موظفي متجر الأقمشة
- ✅ تصميم متجاوب ومتناسق
- ✅ تجربة مستخدم ممتازة

**استمتع بالميزات الجديدة! 🚀**


