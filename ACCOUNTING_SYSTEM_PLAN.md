# 📊 خطة تطوير النظام المحاسبي الشامل لشركة ياسمين الشام

## 📋 فهرس المحتويات

1. [الملخص التنفيذي](#الملخص-التنفيذي)
2. [تحليل الوضع الحالي](#تحليل-الوضع-الحالي)
3. [معمارية النظام المحاسبي المقترحة](#معمارية-النظام-المحاسبي-المقترحة)
4. [مواصفات الميزات التفصيلية](#مواصفات-الميزات-التفصيلية)
5. [تصميم قاعدة البيانات](#تصميم-قاعدة-البيانات)
6. [خارطة طريق التنفيذ](#خارطة-طريق-التنفيذ)
7. [تحليل المخاطر](#تحليل-المخاطر)
8. [استراتيجية الاختبار](#استراتيجية-الاختبار)

---

## 1. الملخص التنفيذي

### 1.1 نظرة عامة على المشروع

**اسم المشروع:** نظام ياسمين الشام المحاسبي المتكامل (Yasmin Al-Sham Integrated Accounting System - YIAS)

**الهدف:** تحويل قسم التقارير الحالي إلى نظام محاسبي عالمي المستوى يتوافق مع المعايير المحاسبية الدولية (IFRS/GAAP)، مع دعم كامل لجميع فروع العمل الثلاثة.

**فروع العمل المدعومة:**
- 🎀 **الملابس الجاهزة (Ready Designs):** بيع التصاميم الجاهزة من المتجر
- 🧵 **الأقمشة (Fabrics):** بيع الأقمشة بالمتر
- ✂️ **التفصيل (Custom Tailoring):** خدمات الخياطة حسب الطلب

### 1.2 الأهداف الاستراتيجية

| الهدف | الوصف | الأولوية |
|-------|-------|----------|
| 📈 **الشفافية المالية** | توفير رؤية شاملة للوضع المالي في الوقت الفعلي | عالية |
| 📊 **الامتثال المحاسبي** | الالتزام بمعايير IFRS/GAAP | عالية |
| 🔗 **التكامل الشامل** | ربط جميع العمليات التجارية بالنظام المحاسبي | عالية |
| 📱 **تجربة المستخدم** | واجهة احترافية سهلة الاستخدام (Mobile-First) | متوسطة |
| 🔐 **الأمان والتدقيق** | سجل تدقيق كامل لجميع المعاملات | عالية |

### 1.3 نطاق المشروع

**ما سيتم تنفيذه:**
- ✅ نظام محاسبي متكامل مع دليل حسابات شامل
- ✅ دفتر الأستاذ العام والقيود اليومية
- ✅ القوائم المالية (الميزانية، الدخل، التدفقات النقدية)
- ✅ نظام الحسابات الدائنة والمدينة
- ✅ محاسبة المخزون والتكاليف
- ✅ تقارير مالية متقدمة
- ✅ دعم تعدد الفروع (Multi-Branch)
- ✅ سجل تدقيق شامل (Audit Trail)

**ما لن يتم تنفيذه في هذه المرحلة:**
- ❌ الرواتب والموارد البشرية (HR/Payroll)
- ❌ إدارة الأصول الثابتة المتقدمة
- ❌ التكامل مع أنظمة خارجية (ERP)

---

## 2. تحليل الوضع الحالي

### 2.1 المعمارية التقنية الحالية

```
┌─────────────────────────────────────────────────────────────────┐
│                      Frontend (Next.js 15)                       │
├─────────────────────────────────────────────────────────────────┤
│  React 19  │  TypeScript  │  TailwindCSS  │  Zustand (State)    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        API Layer                                 │
├─────────────────────────────────────────────────────────────────┤
│  Next.js API Routes  │  Service Layer  │  Supabase Client       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Supabase (Backend)                            │
├─────────────────────────────────────────────────────────────────┤
│  PostgreSQL  │  Row Level Security  │  Auth  │  Storage         │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 الجداول الحالية في قاعدة البيانات

| الجدول | الوصف | الحالة |
|--------|-------|--------|
| `users` | المستخدمين (Admin, Worker, Client) | ✅ موجود |
| `workers` | العمال (الخياطين) | ✅ موجود |
| `appointments` | المواعيد | ✅ موجود |
| `orders` | طلبات التفصيل | ✅ موجود |
| `products` | المنتجات الجاهزة | ✅ موجود |
| `categories` | فئات المنتجات | ✅ موجود |
| `fabrics` | الأقمشة | ✅ موجود |
| `favorites` | المفضلة | ✅ موجود |
| `cart_items` | سلة التسوق | ✅ موجود |

### 2.3 فروع العمل الثلاثة - التحليل التفصيلي

#### 2.3.1 فرع الملابس الجاهزة (Ready Designs)

**العمليات الحالية:**
- عرض المنتجات في المتجر `/designs`
- إضافة للسلة والمفضلة
- إدارة المنتجات من لوحة التحكم `/dashboard/ready-designs`

**نموذج البيانات الحالي (`products`):**
```typescript
interface Product {
  id: string
  title: string
  description: string
  price: number
  is_available: boolean
  stock_quantity: number
  images: string[]
  colors: string[]
  sizes: string[]
  category_name: string
  is_featured: boolean
  is_on_sale: boolean
  sale_price: number | null
}
```

**الفجوات المحاسبية:**
- ❌ لا يوجد تتبع لتكلفة المنتج
- ❌ لا يوجد ربط بفواتير الشراء
- ❌ لا يوجد حساب هامش الربح
- ❌ لا يوجد تتبع لحركة المخزون

#### 2.3.2 فرع الأقمشة (Fabrics)

**العمليات الحالية:**
- عرض الأقمشة في `/fabrics`
- إدارة الأقمشة من `/dashboard/fabrics`
- تصفية وبحث متقدم

**نموذج البيانات الحالي (`fabrics`):**
```typescript
interface Fabric {
  id: string
  name: string
  description: string
  category: string
  price_per_meter: number
  original_price_per_meter: number | null
  is_on_sale: boolean
  discount_percentage: number
  stock_quantity: number  // بالمتر
  available_colors: string[]
  width_cm: number
  is_available: boolean
  is_featured: boolean
}
```

**الفجوات المحاسبية:**
- ❌ لا يوجد تتبع لتكلفة الشراء
- ❌ لا يوجد حساب للهدر والفاقد
- ❌ لا يوجد ربط بين المبيعات والمخزون
- ❌ لا يوجد تقارير ربحية للأقمشة

#### 2.3.3 فرع التفصيل (Custom Tailoring)

**العمليات الحالية:**
- حجز المواعيد `/book-appointment`
- إنشاء طلبات التفصيل `/dashboard/orders`
- تتبع حالة الطلب `/track-order`
- إسناد الطلبات للعمال

**نموذج البيانات الحالي (`orders`):**
```typescript
interface Order {
  id: string
  order_number: string
  user_id: string | null
  worker_id: string | null
  client_name: string
  client_phone: string
  description: string
  fabric: string
  measurements: object
  status: 'pending' | 'in_progress' | 'completed' | 'delivered' | 'cancelled'
  total_price: number
  deposit_amount: number
  payment_status: 'unpaid' | 'partial' | 'paid'
  estimated_delivery_date: string
  actual_delivery_date: string | null
  notes: string
}
```

**الفجوات المحاسبية:**
- ❌ لا يوجد تفصيل لمكونات التكلفة
- ❌ لا يوجد تتبع للمدفوعات المتعددة
- ❌ لا يوجد حساب لأجور العمال
- ❌ لا يوجد ربط بتكلفة القماش المستخدم

### 2.4 تحليل قسم التقارير الحالي

**الموقع:** `src/app/dashboard/reports/page.tsx`

**المؤشرات الحالية:**
| المؤشر | الوصف | الحالة |
|--------|-------|--------|
| إجمالي الإيرادات | مجموع أسعار الطلبات | ⚠️ بسيط |
| عدد الطلبات | إحصاء الطلبات | ✅ يعمل |
| معدل الإنجاز | نسبة الطلبات المكتملة | ✅ يعمل |
| متوسط قيمة الطلب | الإيرادات / عدد الطلبات | ✅ يعمل |
| أداء العمال | إحصائيات لكل عامل | ⚠️ محدود |
| الاتجاه الشهري | رسم بياني 6 أشهر | ⚠️ بسيط |

**نقاط الضعف الرئيسية:**
1. ❌ **لا يوجد نظام محاسبي حقيقي** - مجرد إحصائيات بسيطة
2. ❌ **لا يوجد دليل حسابات** - لا تصنيف للحسابات
3. ❌ **لا يوجد قيود يومية** - لا تسجيل محاسبي
4. ❌ **لا يوجد ميزان مراجعة** - لا تحقق من التوازن
5. ❌ **لا توجد قوائم مالية** - لا ميزانية ولا دخل
6. ❌ **لا يوجد تتبع للتكاليف** - لا حساب للربحية الحقيقية
7. ❌ **لا يوجد نظام فواتير** - لا توثيق للمعاملات
8. ❌ **لا يوجد سجل تدقيق** - لا تتبع للتغييرات

---

## 3. معمارية النظام المحاسبي المقترحة

### 3.1 المعمارية العامة

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         نظام ياسمين الشام المحاسبي                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │   الجاهز    │  │   الأقمشة   │  │   التفصيل   │  │  المشتريات  │        │
│  │  (Designs)  │  │  (Fabrics)  │  │  (Orders)   │  │ (Purchases) │        │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘        │
│         │                │                │                │               │
│         └────────────────┴────────────────┴────────────────┘               │
│                                   │                                         │
│                                   ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    محرك المحاسبة (Accounting Engine)                 │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐        │   │
│  │  │  القيود   │  │  الأستاذ  │  │  الميزان  │  │  القوائم  │        │   │
│  │  │ اليومية   │  │   العام   │  │ المراجعة  │  │  المالية  │        │   │
│  │  └───────────┘  └───────────┘  └───────────┘  └───────────┘        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                   │                                         │
│                                   ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      التقارير والتحليلات                             │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  📊 لوحة المعلومات  │  📈 التقارير المالية  │  🔍 التحليل المتقدم   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 دليل الحسابات (Chart of Accounts)

سيتم اعتماد دليل حسابات متوافق مع المعايير الدولية:

```
1000-1999: الأصول (Assets)
├── 1100-1199: الأصول المتداولة (Current Assets)
│   ├── 1100: النقدية والبنوك
│   │   ├── 1101: الصندوق الرئيسي
│   │   ├── 1102: البنك - الحساب الجاري
│   │   └── 1103: البنك - حساب التوفير
│   ├── 1200: الذمم المدينة (Accounts Receivable)
│   │   ├── 1201: ذمم العملاء - التفصيل
│   │   ├── 1202: ذمم العملاء - الجاهز
│   │   └── 1203: ذمم العملاء - الأقمشة
│   ├── 1300: المخزون (Inventory)
│   │   ├── 1301: مخزون الملابس الجاهزة
│   │   ├── 1302: مخزون الأقمشة
│   │   ├── 1303: مخزون الخيوط والإكسسوارات
│   │   └── 1304: مخزون تحت التشغيل (WIP)
│   └── 1400: المصروفات المدفوعة مقدماً
│
├── 1500-1599: الأصول الثابتة (Fixed Assets)
│   ├── 1501: ماكينات الخياطة
│   ├── 1502: الأثاث والتجهيزات
│   ├── 1503: أجهزة الكمبيوتر
│   └── 1599: مجمع الإهلاك

2000-2999: الالتزامات (Liabilities)
├── 2100-2199: الالتزامات المتداولة
│   ├── 2101: الذمم الدائنة - الموردين
│   ├── 2102: مستحقات العمال
│   ├── 2103: الضرائب المستحقة
│   ├── 2104: العربون المستلم (Customer Deposits)
│   └── 2105: الإيرادات المؤجلة

3000-3999: حقوق الملكية (Equity)
├── 3001: رأس المال
├── 3002: الأرباح المحتجزة
└── 3003: أرباح/خسائر العام الحالي

4000-4999: الإيرادات (Revenue)
├── 4100: إيرادات التفصيل
├── 4200: إيرادات الملابس الجاهزة
├── 4300: إيرادات الأقمشة
├── 4400: إيرادات الخدمات الإضافية
└── 4900: خصومات المبيعات (سالب)

5000-5999: تكلفة المبيعات (COGS)
├── 5100: تكلفة مبيعات التفصيل
│   ├── 5101: تكلفة الأقمشة المستخدمة
│   ├── 5102: تكلفة الخيوط والإكسسوارات
│   └── 5103: أجور العمال المباشرة
├── 5200: تكلفة مبيعات الجاهز
└── 5300: تكلفة مبيعات الأقمشة

6000-6999: المصروفات التشغيلية (Operating Expenses)
├── 6100: الرواتب والأجور
├── 6200: الإيجار
├── 6300: الكهرباء والماء
├── 6400: الصيانة
├── 6500: التسويق والإعلان
├── 6600: مصروفات إدارية
└── 6700: الإهلاك
```


### 3.3 نظام القيود اليومية (Journal Entries)

#### 3.3.1 أنواع القيود التلقائية

| نوع العملية | القيد المدين | القيد الدائن |
|-------------|--------------|--------------|
| **بيع ملابس جاهزة (نقداً)** | 1101 الصندوق | 4200 إيرادات الجاهز |
| | 5200 تكلفة المبيعات | 1301 مخزون الجاهز |
| **بيع أقمشة (نقداً)** | 1101 الصندوق | 4300 إيرادات الأقمشة |
| | 5300 تكلفة المبيعات | 1302 مخزون الأقمشة |
| **طلب تفصيل - استلام عربون** | 1101 الصندوق | 2104 العربون المستلم |
| **طلب تفصيل - تسليم وتحصيل** | 1201 ذمم العملاء | 4100 إيرادات التفصيل |
| | 2104 العربون المستلم | 1201 ذمم العملاء |
| | 1101 الصندوق | 1201 ذمم العملاء |
| **شراء أقمشة** | 1302 مخزون الأقمشة | 2101 الذمم الدائنة |
| **دفع للموردين** | 2101 الذمم الدائنة | 1101 الصندوق |
| **صرف أجور العمال** | 5103 أجور مباشرة | 2102 مستحقات العمال |
| | 2102 مستحقات العمال | 1101 الصندوق |

#### 3.3.2 هيكل القيد اليومي

```typescript
interface JournalEntry {
  id: string
  entry_number: string           // رقم القيد التسلسلي
  entry_date: Date               // تاريخ القيد
  reference_type: 'order' | 'sale' | 'purchase' | 'payment' | 'receipt' | 'adjustment'
  reference_id: string           // معرف المستند المرجعي
  description: string            // وصف القيد
  total_debit: number            // إجمالي المدين
  total_credit: number           // إجمالي الدائن
  status: 'draft' | 'posted' | 'reversed'
  created_by: string             // المستخدم المنشئ
  posted_by: string | null       // المستخدم المرحّل
  posted_at: Date | null         // تاريخ الترحيل
  lines: JournalEntryLine[]      // سطور القيد
}

interface JournalEntryLine {
  id: string
  journal_entry_id: string
  account_id: string             // رقم الحساب
  account_name: string           // اسم الحساب
  debit_amount: number           // المبلغ المدين
  credit_amount: number          // المبلغ الدائن
  description: string            // وصف السطر
  cost_center: string | null     // مركز التكلفة (الفرع)
}
```

### 3.4 نظام الفواتير (Invoicing System)

#### 3.4.1 أنواع الفواتير

| النوع | الوصف | الاستخدام |
|-------|-------|----------|
| **فاتورة مبيعات** | بيع منتجات أو خدمات | الجاهز، الأقمشة، التفصيل |
| **فاتورة مشتريات** | شراء مواد أو خدمات | شراء أقمشة، مستلزمات |
| **إشعار دائن** | إرجاع أو خصم للعميل | المرتجعات |
| **إشعار مدين** | إرجاع للمورد | مرتجعات المشتريات |
| **سند قبض** | استلام نقدية | تحصيل من العملاء |
| **سند صرف** | دفع نقدية | الدفع للموردين |

#### 3.4.2 هيكل الفاتورة

```typescript
interface Invoice {
  id: string
  invoice_number: string         // رقم الفاتورة
  invoice_type: 'sales' | 'purchase' | 'credit_note' | 'debit_note'
  invoice_date: Date
  due_date: Date                 // تاريخ الاستحقاق

  // بيانات الطرف الآخر
  party_type: 'customer' | 'supplier'
  party_id: string
  party_name: string
  party_phone: string
  party_address: string

  // الفرع ومركز التكلفة
  branch: 'tailoring' | 'fabrics' | 'ready_designs'
  cost_center: string

  // المبالغ
  subtotal: number               // المجموع قبل الضريبة
  discount_amount: number        // الخصم
  tax_amount: number             // الضريبة
  total_amount: number           // الإجمالي
  paid_amount: number            // المدفوع
  balance_due: number            // المتبقي

  // الحالة
  status: 'draft' | 'sent' | 'partial' | 'paid' | 'overdue' | 'cancelled'
  payment_status: 'unpaid' | 'partial' | 'paid'

  // الربط المحاسبي
  journal_entry_id: string | null

  // البنود
  items: InvoiceItem[]

  // التتبع
  created_at: Date
  updated_at: Date
  created_by: string
}

interface InvoiceItem {
  id: string
  invoice_id: string
  item_type: 'product' | 'fabric' | 'service' | 'labor'
  item_id: string | null         // معرف المنتج/القماش
  description: string
  quantity: number
  unit: string                   // قطعة، متر، ساعة
  unit_price: number
  discount_percent: number
  discount_amount: number
  tax_percent: number
  tax_amount: number
  total: number
  cost_price: number             // سعر التكلفة (للحساب الداخلي)
}
```

---

## 4. مواصفات الميزات التفصيلية

### 4.1 لوحة المعلومات المحاسبية (Accounting Dashboard)

#### 4.1.1 المؤشرات الرئيسية (KPIs)

| المؤشر | الوصف | التحديث |
|--------|-------|---------|
| 💰 **الرصيد النقدي** | إجمالي النقدية المتاحة | لحظي |
| 📈 **إيرادات اليوم** | مبيعات اليوم الحالي | لحظي |
| 📊 **إيرادات الشهر** | مبيعات الشهر الحالي | يومي |
| 💵 **الذمم المدينة** | المبالغ المستحقة من العملاء | لحظي |
| 💳 **الذمم الدائنة** | المبالغ المستحقة للموردين | لحظي |
| 📦 **قيمة المخزون** | إجمالي قيمة المخزون | يومي |
| 📉 **صافي الربح** | الإيرادات - التكاليف - المصروفات | يومي |
| 🎯 **هامش الربح** | نسبة صافي الربح للإيرادات | يومي |

#### 4.1.2 الرسوم البيانية

1. **مخطط الإيرادات والمصروفات** - خط بياني شهري
2. **توزيع الإيرادات حسب الفرع** - دائري
3. **اتجاه التدفق النقدي** - مساحي
4. **أداء المبيعات اليومي** - أعمدة
5. **مقارنة الفترات** - أعمدة مزدوجة

### 4.2 القوائم المالية (Financial Statements)

#### 4.2.1 الميزانية العمومية (Balance Sheet)

```
┌─────────────────────────────────────────────────────────────────┐
│                    الميزانية العمومية                           │
│                  كما في [التاريخ]                               │
├─────────────────────────────────────────────────────────────────┤
│ الأصول                                                          │
│ ├── الأصول المتداولة                                            │
│ │   ├── النقدية والبنوك ................ XXX                   │
│ │   ├── الذمم المدينة .................. XXX                   │
│ │   ├── المخزون ........................ XXX                   │
│ │   └── مصروفات مدفوعة مقدماً .......... XXX                   │
│ │       ─────────────────────────────────────                   │
│ │       إجمالي الأصول المتداولة ....... XXX                   │
│ │                                                               │
│ ├── الأصول الثابتة                                              │
│ │   ├── الأصول الثابتة (بالتكلفة) ...... XXX                   │
│ │   └── مجمع الإهلاك .................. (XXX)                  │
│ │       ─────────────────────────────────────                   │
│ │       صافي الأصول الثابتة ........... XXX                   │
│ │                                                               │
│ └── إجمالي الأصول ..................... XXX                   │
│                                                                 │
│ الالتزامات وحقوق الملكية                                        │
│ ├── الالتزامات المتداولة                                        │
│ │   ├── الذمم الدائنة .................. XXX                   │
│ │   ├── مستحقات العمال ................. XXX                   │
│ │   ├── العربون المستلم ................ XXX                   │
│ │   └── الضرائب المستحقة ............... XXX                   │
│ │       ─────────────────────────────────────                   │
│ │       إجمالي الالتزامات ............. XXX                   │
│ │                                                               │
│ ├── حقوق الملكية                                                │
│ │   ├── رأس المال ...................... XXX                   │
│ │   ├── الأرباح المحتجزة ............... XXX                   │
│ │   └── صافي ربح العام ................. XXX                   │
│ │       ─────────────────────────────────────                   │
│ │       إجمالي حقوق الملكية ........... XXX                   │
│ │                                                               │
│ └── إجمالي الالتزامات وحقوق الملكية ... XXX                   │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.2.2 قائمة الدخل (Income Statement)

```
┌─────────────────────────────────────────────────────────────────┐
│                      قائمة الدخل                                │
│              للفترة من [تاريخ] إلى [تاريخ]                      │
├─────────────────────────────────────────────────────────────────┤
│ الإيرادات                                                       │
│ ├── إيرادات التفصيل .................... XXX                   │
│ ├── إيرادات الملابس الجاهزة ............ XXX                   │
│ ├── إيرادات الأقمشة .................... XXX                   │
│ └── خصومات المبيعات ................... (XXX)                  │
│     ─────────────────────────────────────────                   │
│     صافي الإيرادات .................... XXX                   │
│                                                                 │
│ تكلفة المبيعات                                                  │
│ ├── تكلفة مبيعات التفصيل ............... XXX                   │
│ ├── تكلفة مبيعات الجاهز ................ XXX                   │
│ └── تكلفة مبيعات الأقمشة ............... XXX                   │
│     ─────────────────────────────────────────                   │
│     إجمالي تكلفة المبيعات ............. XXX                   │
│                                                                 │
│ مجمل الربح ............................ XXX                   │
│                                                                 │
│ المصروفات التشغيلية                                             │
│ ├── الرواتب والأجور .................... XXX                   │
│ ├── الإيجار ............................ XXX                   │
│ ├── الكهرباء والماء .................... XXX                   │
│ ├── الصيانة ............................ XXX                   │
│ ├── التسويق والإعلان ................... XXX                   │
│ ├── مصروفات إدارية ..................... XXX                   │
│ └── الإهلاك ............................ XXX                   │
│     ─────────────────────────────────────────                   │
│     إجمالي المصروفات التشغيلية ........ XXX                   │
│                                                                 │
│ صافي الربح التشغيلي ................... XXX                   │
│                                                                 │
│ إيرادات ومصروفات أخرى                                           │
│ ├── إيرادات أخرى ....................... XXX                   │
│ └── مصروفات أخرى ...................... (XXX)                  │
│                                                                 │
│ صافي الربح قبل الضريبة ................ XXX                   │
│ ضريبة الدخل .......................... (XXX)                  │
│     ─────────────────────────────────────────                   │
│ صافي الربح ............................ XXX                   │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.2.3 قائمة التدفقات النقدية (Cash Flow Statement)

```
┌─────────────────────────────────────────────────────────────────┐
│                   قائمة التدفقات النقدية                        │
│              للفترة من [تاريخ] إلى [تاريخ]                      │
├─────────────────────────────────────────────────────────────────┤
│ التدفقات النقدية من الأنشطة التشغيلية                           │
│ ├── صافي الربح ......................... XXX                   │
│ ├── تعديلات:                                                    │
│ │   ├── الإهلاك ........................ XXX                   │
│ │   ├── (زيادة)/نقص الذمم المدينة ...... XXX                   │
│ │   ├── (زيادة)/نقص المخزون ............ XXX                   │
│ │   └── زيادة/(نقص) الذمم الدائنة ...... XXX                   │
│     ─────────────────────────────────────────                   │
│     صافي التدفق من الأنشطة التشغيلية .. XXX                   │
│                                                                 │
│ التدفقات النقدية من الأنشطة الاستثمارية                         │
│ ├── شراء أصول ثابتة ................... (XXX)                  │
│ └── بيع أصول ثابتة ..................... XXX                   │
│     ─────────────────────────────────────────                   │
│     صافي التدفق من الأنشطة الاستثمارية  XXX                   │
│                                                                 │
│ التدفقات النقدية من الأنشطة التمويلية                           │
│ ├── زيادة رأس المال .................... XXX                   │
│ └── توزيعات الأرباح ................... (XXX)                  │
│     ─────────────────────────────────────────                   │
│     صافي التدفق من الأنشطة التمويلية .. XXX                   │
│                                                                 │
│ صافي التغير في النقدية ................ XXX                   │
│ رصيد النقدية أول الفترة ............... XXX                   │
│     ─────────────────────────────────────────                   │
│ رصيد النقدية آخر الفترة ............... XXX                   │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 نظام المخزون المحاسبي (Inventory Accounting)

#### 4.3.1 طرق تقييم المخزون

| الطريقة | الوصف | الاستخدام |
|---------|-------|----------|
| **المتوسط المرجح** | متوسط تكلفة جميع الوحدات | الأقمشة (افتراضي) |
| **FIFO** | الوارد أولاً صادر أولاً | الملابس الجاهزة |
| **التكلفة المحددة** | تكلفة كل وحدة بعينها | القطع الفريدة |

#### 4.3.2 حركات المخزون

```typescript
interface InventoryMovement {
  id: string
  movement_type: 'purchase' | 'sale' | 'adjustment' | 'transfer' | 'return' | 'waste'
  item_type: 'product' | 'fabric' | 'material'
  item_id: string
  quantity: number
  unit_cost: number
  total_cost: number
  reference_type: 'invoice' | 'order' | 'adjustment'
  reference_id: string
  notes: string
  created_at: Date
  created_by: string
}
```


### 4.4 نظام الحسابات الدائنة والمدينة (AP/AR)

#### 4.4.1 إدارة الذمم المدينة (Accounts Receivable)

**الميزات:**
- 📋 قائمة العملاء مع أرصدتهم
- 📊 تقادم الذمم (Aging Report): 0-30، 31-60، 61-90، +90 يوم
- 🔔 تنبيهات الاستحقاق
- 📧 إرسال تذكيرات للعملاء
- 💳 تسجيل المدفوعات وربطها بالفواتير
- 📈 تقارير التحصيل

```typescript
interface CustomerAccount {
  id: string
  customer_id: string
  customer_name: string
  customer_phone: string
  total_invoiced: number      // إجمالي الفواتير
  total_paid: number          // إجمالي المدفوع
  balance: number             // الرصيد المستحق
  credit_limit: number        // حد الائتمان
  payment_terms: number       // أيام السداد
  last_payment_date: Date | null
  status: 'active' | 'on_hold' | 'blocked'
}
```

#### 4.4.2 إدارة الذمم الدائنة (Accounts Payable)

**الميزات:**
- 📋 قائمة الموردين مع أرصدتهم
- 📊 تقادم المستحقات
- 📅 جدولة الدفعات
- 💳 تسجيل المدفوعات
- 📈 تقارير المشتريات

```typescript
interface SupplierAccount {
  id: string
  supplier_id: string
  supplier_name: string
  supplier_phone: string
  total_purchases: number     // إجمالي المشتريات
  total_paid: number          // إجمالي المدفوع
  balance: number             // الرصيد المستحق
  payment_terms: number       // أيام السداد
  last_payment_date: Date | null
}
```

### 4.5 محاسبة التكاليف (Cost Accounting)

#### 4.5.1 تكلفة طلب التفصيل

```typescript
interface OrderCostBreakdown {
  order_id: string

  // تكاليف المواد المباشرة
  fabric_cost: number           // تكلفة القماش
  fabric_quantity_meters: number
  accessories_cost: number      // الإكسسوارات
  thread_cost: number           // الخيوط

  // تكاليف العمالة المباشرة
  labor_hours: number
  labor_rate: number
  labor_cost: number

  // التكاليف غير المباشرة (نسبة)
  overhead_rate: number
  overhead_cost: number

  // الإجمالي
  total_cost: number
  selling_price: number
  gross_profit: number
  profit_margin: number
}
```

#### 4.5.2 مراكز التكلفة

| مركز التكلفة | الكود | الوصف |
|--------------|-------|-------|
| التفصيل | CC-001 | قسم الخياطة حسب الطلب |
| الجاهز | CC-002 | قسم الملابس الجاهزة |
| الأقمشة | CC-003 | قسم بيع الأقمشة |
| الإدارة | CC-004 | المصروفات الإدارية |
| التسويق | CC-005 | مصروفات التسويق |

### 4.6 التقارير المالية المتقدمة

#### 4.6.1 قائمة التقارير

| التقرير | الوصف | التكرار |
|---------|-------|---------|
| **ميزان المراجعة** | أرصدة جميع الحسابات | يومي/شهري |
| **دفتر الأستاذ** | حركات كل حساب | حسب الطلب |
| **يومية القيود** | جميع القيود المحاسبية | يومي |
| **تقرير المبيعات** | تحليل المبيعات بالتفصيل | يومي/أسبوعي |
| **تقرير المشتريات** | تحليل المشتريات | أسبوعي/شهري |
| **تقرير المخزون** | حركة وقيمة المخزون | أسبوعي |
| **تقرير الربحية** | تحليل الربحية بالفرع | شهري |
| **تقرير التدفق النقدي** | حركة النقدية | يومي/أسبوعي |
| **تقرير تقادم الذمم** | عمر الديون | أسبوعي |
| **تقرير أداء العمال** | إنتاجية وتكلفة العمال | شهري |

#### 4.6.2 خيارات التصدير

- 📄 PDF (للطباعة والأرشفة)
- 📊 Excel (للتحليل)
- 📁 CSV (للتكامل)
- 🖨️ طباعة مباشرة

---

## 5. تصميم قاعدة البيانات

### 5.1 الجداول الجديدة المطلوبة

#### 5.1.1 جدول دليل الحسابات (accounts)

```sql
CREATE TABLE accounts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  account_code VARCHAR(10) UNIQUE NOT NULL,
  account_name VARCHAR(100) NOT NULL,
  account_name_en VARCHAR(100),
  account_type VARCHAR(20) NOT NULL CHECK (account_type IN ('asset', 'liability', 'equity', 'revenue', 'expense')),
  parent_account_id UUID REFERENCES accounts(id),
  level INTEGER NOT NULL DEFAULT 1,
  is_active BOOLEAN DEFAULT true,
  is_system BOOLEAN DEFAULT false,  -- حسابات النظام لا يمكن حذفها
  normal_balance VARCHAR(10) CHECK (normal_balance IN ('debit', 'credit')),
  description TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- فهارس
CREATE INDEX idx_accounts_code ON accounts(account_code);
CREATE INDEX idx_accounts_type ON accounts(account_type);
CREATE INDEX idx_accounts_parent ON accounts(parent_account_id);
```

#### 5.1.2 جدول القيود اليومية (journal_entries)

```sql
CREATE TABLE journal_entries (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  entry_number VARCHAR(20) UNIQUE NOT NULL,
  entry_date DATE NOT NULL,
  reference_type VARCHAR(20) NOT NULL,
  reference_id UUID,
  description TEXT NOT NULL,
  total_debit DECIMAL(15, 2) NOT NULL DEFAULT 0,
  total_credit DECIMAL(15, 2) NOT NULL DEFAULT 0,
  status VARCHAR(20) DEFAULT 'draft' CHECK (status IN ('draft', 'posted', 'reversed')),
  fiscal_year INTEGER NOT NULL,
  fiscal_period INTEGER NOT NULL,
  created_by UUID REFERENCES users(id),
  posted_by UUID REFERENCES users(id),
  posted_at TIMESTAMPTZ,
  reversed_by UUID REFERENCES users(id),
  reversed_at TIMESTAMPTZ,
  reversal_entry_id UUID REFERENCES journal_entries(id),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),

  CONSTRAINT check_balanced CHECK (total_debit = total_credit)
);

-- فهارس
CREATE INDEX idx_journal_entries_date ON journal_entries(entry_date);
CREATE INDEX idx_journal_entries_status ON journal_entries(status);
CREATE INDEX idx_journal_entries_reference ON journal_entries(reference_type, reference_id);
```

#### 5.1.3 جدول سطور القيود (journal_entry_lines)

```sql
CREATE TABLE journal_entry_lines (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  journal_entry_id UUID NOT NULL REFERENCES journal_entries(id) ON DELETE CASCADE,
  account_id UUID NOT NULL REFERENCES accounts(id),
  debit_amount DECIMAL(15, 2) DEFAULT 0,
  credit_amount DECIMAL(15, 2) DEFAULT 0,
  description TEXT,
  cost_center VARCHAR(20),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- فهارس
CREATE INDEX idx_jel_entry ON journal_entry_lines(journal_entry_id);
CREATE INDEX idx_jel_account ON journal_entry_lines(account_id);
CREATE INDEX idx_jel_cost_center ON journal_entry_lines(cost_center);
```


#### 5.1.4 جدول الفواتير (invoices)

```sql
CREATE TABLE invoices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_number VARCHAR(20) UNIQUE NOT NULL,
  invoice_type VARCHAR(20) NOT NULL CHECK (invoice_type IN ('sales', 'purchase', 'credit_note', 'debit_note')),
  invoice_date DATE NOT NULL,
  due_date DATE NOT NULL,

  -- الطرف الآخر
  party_type VARCHAR(20) NOT NULL CHECK (party_type IN ('customer', 'supplier')),
  party_id UUID,
  party_name VARCHAR(100) NOT NULL,
  party_phone VARCHAR(20),
  party_address TEXT,

  -- الفرع
  branch VARCHAR(20) NOT NULL CHECK (branch IN ('tailoring', 'fabrics', 'ready_designs')),
  cost_center VARCHAR(20),

  -- المبالغ
  subtotal DECIMAL(15, 2) NOT NULL DEFAULT 0,
  discount_amount DECIMAL(15, 2) DEFAULT 0,
  tax_amount DECIMAL(15, 2) DEFAULT 0,
  total_amount DECIMAL(15, 2) NOT NULL DEFAULT 0,
  paid_amount DECIMAL(15, 2) DEFAULT 0,
  balance_due DECIMAL(15, 2) GENERATED ALWAYS AS (total_amount - paid_amount) STORED,

  -- الحالة
  status VARCHAR(20) DEFAULT 'draft' CHECK (status IN ('draft', 'sent', 'partial', 'paid', 'overdue', 'cancelled')),
  payment_status VARCHAR(20) DEFAULT 'unpaid' CHECK (payment_status IN ('unpaid', 'partial', 'paid')),

  -- الربط
  order_id UUID REFERENCES orders(id),
  journal_entry_id UUID REFERENCES journal_entries(id),

  -- التتبع
  notes TEXT,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- فهارس
CREATE INDEX idx_invoices_number ON invoices(invoice_number);
CREATE INDEX idx_invoices_date ON invoices(invoice_date);
CREATE INDEX idx_invoices_party ON invoices(party_type, party_id);
CREATE INDEX idx_invoices_status ON invoices(status);
CREATE INDEX idx_invoices_branch ON invoices(branch);
```

#### 5.1.5 جدول بنود الفواتير (invoice_items)

```sql
CREATE TABLE invoice_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_id UUID NOT NULL REFERENCES invoices(id) ON DELETE CASCADE,
  item_type VARCHAR(20) NOT NULL CHECK (item_type IN ('product', 'fabric', 'service', 'labor', 'other')),
  item_id UUID,
  description TEXT NOT NULL,
  quantity DECIMAL(10, 3) NOT NULL,
  unit VARCHAR(20) DEFAULT 'piece',
  unit_price DECIMAL(15, 2) NOT NULL,
  discount_percent DECIMAL(5, 2) DEFAULT 0,
  discount_amount DECIMAL(15, 2) DEFAULT 0,
  tax_percent DECIMAL(5, 2) DEFAULT 0,
  tax_amount DECIMAL(15, 2) DEFAULT 0,
  total DECIMAL(15, 2) NOT NULL,
  cost_price DECIMAL(15, 2) DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- فهارس
CREATE INDEX idx_invoice_items_invoice ON invoice_items(invoice_id);
CREATE INDEX idx_invoice_items_item ON invoice_items(item_type, item_id);
```

#### 5.1.6 جدول المدفوعات (payments)

```sql
CREATE TABLE payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  payment_number VARCHAR(20) UNIQUE NOT NULL,
  payment_type VARCHAR(20) NOT NULL CHECK (payment_type IN ('receipt', 'payment')),
  payment_date DATE NOT NULL,

  -- الطرف
  party_type VARCHAR(20) NOT NULL CHECK (party_type IN ('customer', 'supplier')),
  party_id UUID,
  party_name VARCHAR(100) NOT NULL,

  -- المبلغ
  amount DECIMAL(15, 2) NOT NULL,
  payment_method VARCHAR(20) NOT NULL CHECK (payment_method IN ('cash', 'bank_transfer', 'card', 'check')),

  -- الربط
  invoice_id UUID REFERENCES invoices(id),
  order_id UUID REFERENCES orders(id),
  journal_entry_id UUID REFERENCES journal_entries(id),

  -- التفاصيل
  reference_number VARCHAR(50),
  notes TEXT,

  -- التتبع
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- فهارس
CREATE INDEX idx_payments_date ON payments(payment_date);
CREATE INDEX idx_payments_party ON payments(party_type, party_id);
CREATE INDEX idx_payments_invoice ON payments(invoice_id);
```

#### 5.1.7 جدول الموردين (suppliers)

```sql
CREATE TABLE suppliers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  supplier_code VARCHAR(20) UNIQUE NOT NULL,
  name VARCHAR(100) NOT NULL,
  contact_person VARCHAR(100),
  phone VARCHAR(20),
  email VARCHAR(100),
  address TEXT,
  tax_number VARCHAR(50),
  payment_terms INTEGER DEFAULT 30,
  credit_limit DECIMAL(15, 2) DEFAULT 0,
  balance DECIMAL(15, 2) DEFAULT 0,
  is_active BOOLEAN DEFAULT true,
  notes TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- فهارس
CREATE INDEX idx_suppliers_code ON suppliers(supplier_code);
CREATE INDEX idx_suppliers_name ON suppliers(name);
```

#### 5.1.8 جدول حركات المخزون (inventory_movements)

```sql
CREATE TABLE inventory_movements (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  movement_type VARCHAR(20) NOT NULL CHECK (movement_type IN ('purchase', 'sale', 'adjustment', 'transfer', 'return', 'waste')),
  item_type VARCHAR(20) NOT NULL CHECK (item_type IN ('product', 'fabric', 'material')),
  item_id UUID NOT NULL,
  quantity DECIMAL(10, 3) NOT NULL,
  unit_cost DECIMAL(15, 2) NOT NULL,
  total_cost DECIMAL(15, 2) NOT NULL,
  quantity_before DECIMAL(10, 3) NOT NULL,
  quantity_after DECIMAL(10, 3) NOT NULL,
  reference_type VARCHAR(20),
  reference_id UUID,
  notes TEXT,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- فهارس
CREATE INDEX idx_inventory_movements_item ON inventory_movements(item_type, item_id);
CREATE INDEX idx_inventory_movements_date ON inventory_movements(created_at);
CREATE INDEX idx_inventory_movements_type ON inventory_movements(movement_type);
```

#### 5.1.9 جدول سجل التدقيق (audit_log)

```sql
CREATE TABLE audit_log (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  table_name VARCHAR(50) NOT NULL,
  record_id UUID NOT NULL,
  action VARCHAR(20) NOT NULL CHECK (action IN ('INSERT', 'UPDATE', 'DELETE')),
  old_values JSONB,
  new_values JSONB,
  changed_fields TEXT[],
  user_id UUID REFERENCES users(id),
  user_name VARCHAR(100),
  ip_address VARCHAR(50),
  user_agent TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- فهارس
CREATE INDEX idx_audit_log_table ON audit_log(table_name);
CREATE INDEX idx_audit_log_record ON audit_log(record_id);
CREATE INDEX idx_audit_log_date ON audit_log(created_at);
CREATE INDEX idx_audit_log_user ON audit_log(user_id);
```


### 5.2 التعديلات على الجداول الحالية

#### 5.2.1 تعديلات جدول orders

```sql
-- إضافة حقول محاسبية لجدول الطلبات
ALTER TABLE orders ADD COLUMN IF NOT EXISTS cost_breakdown JSONB DEFAULT '{}';
ALTER TABLE orders ADD COLUMN IF NOT EXISTS fabric_cost DECIMAL(15, 2) DEFAULT 0;
ALTER TABLE orders ADD COLUMN IF NOT EXISTS labor_cost DECIMAL(15, 2) DEFAULT 0;
ALTER TABLE orders ADD COLUMN IF NOT EXISTS accessories_cost DECIMAL(15, 2) DEFAULT 0;
ALTER TABLE orders ADD COLUMN IF NOT EXISTS overhead_cost DECIMAL(15, 2) DEFAULT 0;
ALTER TABLE orders ADD COLUMN IF NOT EXISTS total_cost DECIMAL(15, 2) DEFAULT 0;
ALTER TABLE orders ADD COLUMN IF NOT EXISTS profit_margin DECIMAL(5, 2) DEFAULT 0;
ALTER TABLE orders ADD COLUMN IF NOT EXISTS invoice_id UUID REFERENCES invoices(id);
```

#### 5.2.2 تعديلات جدول products

```sql
-- إضافة حقول التكلفة للمنتجات الجاهزة
ALTER TABLE products ADD COLUMN IF NOT EXISTS cost_price DECIMAL(15, 2) DEFAULT 0;
ALTER TABLE products ADD COLUMN IF NOT EXISTS supplier_id UUID REFERENCES suppliers(id);
ALTER TABLE products ADD COLUMN IF NOT EXISTS last_purchase_date DATE;
ALTER TABLE products ADD COLUMN IF NOT EXISTS last_purchase_price DECIMAL(15, 2);
ALTER TABLE products ADD COLUMN IF NOT EXISTS average_cost DECIMAL(15, 2) DEFAULT 0;
```

#### 5.2.3 تعديلات جدول fabrics

```sql
-- إضافة حقول التكلفة للأقمشة
ALTER TABLE fabrics ADD COLUMN IF NOT EXISTS cost_per_meter DECIMAL(15, 2) DEFAULT 0;
ALTER TABLE fabrics ADD COLUMN IF NOT EXISTS supplier_id UUID REFERENCES suppliers(id);
ALTER TABLE fabrics ADD COLUMN IF NOT EXISTS last_purchase_date DATE;
ALTER TABLE fabrics ADD COLUMN IF NOT EXISTS last_purchase_price DECIMAL(15, 2);
ALTER TABLE fabrics ADD COLUMN IF NOT EXISTS average_cost DECIMAL(15, 2) DEFAULT 0;
ALTER TABLE fabrics ADD COLUMN IF NOT EXISTS reorder_level DECIMAL(10, 2) DEFAULT 0;
```

### 5.3 مخطط العلاقات (ERD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          مخطط العلاقات المحاسبية                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────┐     ┌──────────────┐     ┌─────────────────┐                 │
│  │ accounts │◄────│journal_entry │────►│journal_entry    │                 │
│  │          │     │   _lines     │     │                 │                 │
│  └──────────┘     └──────────────┘     └────────┬────────┘                 │
│       ▲                                         │                          │
│       │                                         │                          │
│       │           ┌─────────────────────────────┘                          │
│       │           │                                                        │
│       │           ▼                                                        │
│  ┌────┴─────┐  ┌──────────┐  ┌──────────────┐  ┌──────────┐               │
│  │ invoices │◄─│ payments │  │invoice_items │  │ orders   │               │
│  │          │  │          │  │              │  │          │               │
│  └────┬─────┘  └──────────┘  └──────────────┘  └────┬─────┘               │
│       │                                             │                      │
│       │        ┌────────────────────────────────────┘                      │
│       │        │                                                           │
│       ▼        ▼                                                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────────────┐          │
│  │suppliers │  │ products │  │ fabrics  │  │inventory_movements│          │
│  │          │  │          │  │          │  │                   │          │
│  └──────────┘  └──────────┘  └──────────┘  └───────────────────┘          │
│                                                                             │
│  ┌──────────┐  ┌──────────┐                                                │
│  │  users   │  │audit_log │                                                │
│  │          │  │          │                                                │
│  └──────────┘  └──────────┘                                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 6. خارطة طريق التنفيذ

### 6.1 نظرة عامة على المراحل

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         خارطة طريق التنفيذ                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  المرحلة 1        المرحلة 2        المرحلة 3        المرحلة 4              │
│  ──────────       ──────────       ──────────       ──────────              │
│  البنية التحتية   النظام الأساسي   التكامل          التقارير المتقدمة       │
│                                                                             │
│  ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐               │
│  │ أسبوعان │────►│ 4 أسابيع│────►│ 3 أسابيع│────►│ 3 أسابيع│               │
│  └─────────┘     └─────────┘     └─────────┘     └─────────┘               │
│                                                                             │
│  • قاعدة البيانات • دليل الحسابات • ربط الطلبات   • القوائم المالية         │
│  • الخدمات        • القيود اليومية • ربط المبيعات  • التقارير التحليلية     │
│  • الأنواع        • الفواتير      • ربط المخزون   • لوحة المعلومات          │
│  • الاختبارات     • المدفوعات     • سجل التدقيق   • التصدير                 │
│                                                                             │
│  ═══════════════════════════════════════════════════════════════════════   │
│                        إجمالي المدة: 12 أسبوعاً                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 المرحلة الأولى: البنية التحتية (أسبوعان)

#### الأسبوع 1: قاعدة البيانات والأنواع

| المهمة | الوصف | الأولوية |
|--------|-------|----------|
| إنشاء ملف الهجرة | `migrations/10-accounting-system.sql` | عالية |
| إنشاء جدول accounts | دليل الحسابات | عالية |
| إنشاء جدول journal_entries | القيود اليومية | عالية |
| إنشاء جدول journal_entry_lines | سطور القيود | عالية |
| إنشاء جدول invoices | الفواتير | عالية |
| إنشاء جدول invoice_items | بنود الفواتير | عالية |
| إنشاء جدول payments | المدفوعات | عالية |
| إنشاء جدول suppliers | الموردين | متوسطة |
| إنشاء جدول inventory_movements | حركات المخزون | متوسطة |
| إنشاء جدول audit_log | سجل التدقيق | متوسطة |
| تعديل الجداول الحالية | إضافة الحقول المحاسبية | عالية |

#### الأسبوع 2: الخدمات والأنواع

| المهمة | الوصف | الأولوية |
|--------|-------|----------|
| إنشاء `src/types/accounting.ts` | أنواع TypeScript | عالية |
| إنشاء `src/lib/services/accounting-service.ts` | خدمة المحاسبة الرئيسية | عالية |
| إنشاء `src/lib/services/journal-service.ts` | خدمة القيود | عالية |
| إنشاء `src/lib/services/invoice-service.ts` | خدمة الفواتير | عالية |
| إنشاء `src/lib/services/payment-service.ts` | خدمة المدفوعات | عالية |
| إنشاء `src/store/accountingStore.ts` | Zustand Store | عالية |
| كتابة اختبارات الوحدة | Jest tests | متوسطة |

### 6.3 المرحلة الثانية: النظام الأساسي (4 أسابيع)

#### الأسبوع 3-4: دليل الحسابات والقيود

| المهمة | الوصف | الأولوية |
|--------|-------|----------|
| صفحة دليل الحسابات | `/dashboard/accounting/accounts` | عالية |
| إضافة/تعديل/حذف حسابات | CRUD operations | عالية |
| صفحة القيود اليومية | `/dashboard/accounting/journal` | عالية |
| إنشاء قيد يدوي | مع التحقق من التوازن | عالية |
| ترحيل القيود | Post entries | عالية |
| عكس القيود | Reverse entries | متوسطة |
| البحث والفلترة | بالتاريخ والحساب | عالية |

#### الأسبوع 5-6: الفواتير والمدفوعات

| المهمة | الوصف | الأولوية |
|--------|-------|----------|
| صفحة الفواتير | `/dashboard/accounting/invoices` | عالية |
| إنشاء فاتورة مبيعات | مع البنود | عالية |
| إنشاء فاتورة مشتريات | مع البنود | عالية |
| طباعة الفاتورة | PDF generation | متوسطة |
| صفحة المدفوعات | `/dashboard/accounting/payments` | عالية |
| تسجيل سند قبض | من العملاء | عالية |
| تسجيل سند صرف | للموردين | عالية |
| ربط المدفوعات بالفواتير | Payment allocation | عالية |


### 6.4 المرحلة الثالثة: التكامل (3 أسابيع)

#### الأسبوع 7-8: ربط العمليات التجارية

| المهمة | الوصف | الأولوية |
|--------|-------|----------|
| ربط طلبات التفصيل | إنشاء قيود تلقائية | عالية |
| ربط مبيعات الجاهز | إنشاء فواتير وقيود | عالية |
| ربط مبيعات الأقمشة | إنشاء فواتير وقيود | عالية |
| تتبع حركة المخزون | تسجيل تلقائي | عالية |
| حساب التكاليف | تكلفة الطلبات | عالية |
| إدارة الموردين | صفحة الموردين | متوسطة |

#### الأسبوع 9: سجل التدقيق والأمان

| المهمة | الوصف | الأولوية |
|--------|-------|----------|
| تفعيل سجل التدقيق | Triggers للجداول | عالية |
| صفحة سجل التدقيق | `/dashboard/accounting/audit` | متوسطة |
| صلاحيات المحاسبة | Role-based access | عالية |
| التحقق من البيانات | Validation rules | عالية |

### 6.5 المرحلة الرابعة: التقارير المتقدمة (3 أسابيع)

#### الأسبوع 10-11: القوائم المالية

| المهمة | الوصف | الأولوية |
|--------|-------|----------|
| ميزان المراجعة | Trial Balance | عالية |
| دفتر الأستاذ | General Ledger | عالية |
| الميزانية العمومية | Balance Sheet | عالية |
| قائمة الدخل | Income Statement | عالية |
| قائمة التدفقات النقدية | Cash Flow | متوسطة |
| تقرير تقادم الذمم | Aging Report | عالية |

#### الأسبوع 12: لوحة المعلومات والتصدير

| المهمة | الوصف | الأولوية |
|--------|-------|----------|
| لوحة المعلومات المحاسبية | Dashboard | عالية |
| الرسوم البيانية | Charts | عالية |
| تصدير PDF | jsPDF | عالية |
| تصدير Excel | xlsx | عالية |
| الطباعة | Print styles | متوسطة |
| الاختبارات النهائية | E2E tests | عالية |

---

## 7. تحليل المخاطر

### 7.1 مصفوفة المخاطر

| المخاطرة | الاحتمالية | التأثير | الدرجة | خطة التخفيف |
|----------|-----------|---------|--------|-------------|
| **تعقيد التكامل** | عالية | عالي | 🔴 حرج | تقسيم التكامل لمراحل صغيرة، اختبار مكثف |
| **أخطاء في القيود** | متوسطة | عالي | 🟠 مرتفع | التحقق من التوازن، مراجعة قبل الترحيل |
| **فقدان البيانات** | منخفضة | عالي | 🟡 متوسط | نسخ احتياطي يومي، سجل تدقيق |
| **أداء بطيء** | متوسطة | متوسط | 🟡 متوسط | فهارس محسنة، تحميل تدريجي |
| **صعوبة الاستخدام** | متوسطة | متوسط | 🟡 متوسط | تصميم UX احترافي، تدريب المستخدمين |
| **عدم توافق البيانات** | منخفضة | عالي | 🟡 متوسط | هجرة تدريجية، التحقق من البيانات |
| **تأخر الجدول الزمني** | متوسطة | متوسط | 🟡 متوسط | مراجعة أسبوعية، مرونة في الأولويات |

### 7.2 خطط الطوارئ

#### 7.2.1 في حالة تعقيد التكامل

```
1. تأجيل الميزات غير الأساسية
2. التركيز على التكامل اليدوي أولاً
3. إضافة التكامل التلقائي تدريجياً
4. توفير أدوات استيراد/تصدير
```

#### 7.2.2 في حالة أخطاء القيود

```
1. منع الترحيل التلقائي في البداية
2. مراجعة يدوية لجميع القيود
3. تقارير يومية للتحقق من التوازن
4. آلية عكس القيود الخاطئة
```

#### 7.2.3 في حالة مشاكل الأداء

```
1. تحسين الاستعلامات
2. إضافة فهارس إضافية
3. تقسيم البيانات (Pagination)
4. التخزين المؤقت (Caching)
```

---

## 8. استراتيجية الاختبار

### 8.1 أنواع الاختبارات

| النوع | الأداة | التغطية المستهدفة |
|-------|--------|-------------------|
| **اختبارات الوحدة** | Jest | 80%+ |
| **اختبارات التكامل** | Jest + Supabase | 70%+ |
| **اختبارات E2E** | Playwright | المسارات الحرجة |
| **اختبارات الأداء** | Lighthouse | Core Web Vitals |

### 8.2 سيناريوهات الاختبار الحرجة

#### 8.2.1 اختبارات القيود اليومية

```typescript
describe('Journal Entries', () => {
  test('يجب أن يكون القيد متوازناً', async () => {
    // المدين = الدائن
  })

  test('لا يمكن ترحيل قيد غير متوازن', async () => {
    // رفض القيد
  })

  test('يمكن عكس قيد مرحّل', async () => {
    // إنشاء قيد عكسي
  })

  test('تحديث أرصدة الحسابات بعد الترحيل', async () => {
    // التحقق من الأرصدة
  })
})
```

#### 8.2.2 اختبارات الفواتير

```typescript
describe('Invoices', () => {
  test('إنشاء فاتورة مبيعات', async () => {
    // إنشاء فاتورة مع بنود
  })

  test('إنشاء قيد تلقائي للفاتورة', async () => {
    // التحقق من القيد
  })

  test('تسجيل دفعة وتحديث الرصيد', async () => {
    // التحقق من الرصيد المتبقي
  })
})
```

#### 8.2.3 اختبارات التقارير

```typescript
describe('Financial Reports', () => {
  test('ميزان المراجعة متوازن', async () => {
    // مجموع المدين = مجموع الدائن
  })

  test('الميزانية: الأصول = الالتزامات + حقوق الملكية', async () => {
    // معادلة المحاسبة
  })

  test('قائمة الدخل صحيحة', async () => {
    // الإيرادات - المصروفات = صافي الربح
  })
})
```

### 8.3 معايير القبول

| المعيار | الحد الأدنى | المستهدف |
|---------|------------|----------|
| تغطية الاختبارات | 70% | 85% |
| وقت تحميل الصفحة | < 3 ثواني | < 1.5 ثانية |
| دقة الحسابات | 100% | 100% |
| توازن القيود | 100% | 100% |
| توافق المتصفحات | Chrome, Firefox, Safari | + Edge, Mobile |

---

## 9. الملحقات

### 9.1 قائمة الملفات المطلوب إنشاؤها

```
📁 migrations/
├── 10-accounting-system.sql

📁 src/types/
├── accounting.ts

📁 src/lib/services/
├── accounting-service.ts
├── journal-service.ts
├── invoice-service.ts
├── payment-service.ts
├── supplier-service.ts
├── inventory-service.ts
├── report-service.ts

📁 src/store/
├── accountingStore.ts

📁 src/app/dashboard/accounting/
├── page.tsx                    (لوحة المعلومات)
├── accounts/
│   └── page.tsx               (دليل الحسابات)
├── journal/
│   ├── page.tsx               (القيود اليومية)
│   └── [id]/page.tsx          (تفاصيل القيد)
├── invoices/
│   ├── page.tsx               (الفواتير)
│   ├── new/page.tsx           (فاتورة جديدة)
│   └── [id]/page.tsx          (تفاصيل الفاتورة)
├── payments/
│   └── page.tsx               (المدفوعات)
├── suppliers/
│   └── page.tsx               (الموردين)
├── reports/
│   ├── page.tsx               (التقارير)
│   ├── trial-balance/page.tsx (ميزان المراجعة)
│   ├── ledger/page.tsx        (دفتر الأستاذ)
│   ├── balance-sheet/page.tsx (الميزانية)
│   ├── income/page.tsx        (قائمة الدخل)
│   └── cash-flow/page.tsx     (التدفقات النقدية)
└── audit/
    └── page.tsx               (سجل التدقيق)

📁 src/components/accounting/
├── AccountTree.tsx
├── JournalEntryForm.tsx
├── InvoiceForm.tsx
├── PaymentForm.tsx
├── FinancialChart.tsx
├── ReportViewer.tsx
└── AuditLogTable.tsx
```

### 9.2 المراجع والمعايير

| المعيار | الوصف | الرابط |
|---------|-------|--------|
| IFRS | المعايير الدولية للتقارير المالية | ifrs.org |
| GAAP | مبادئ المحاسبة المقبولة عموماً | fasb.org |
| Double-Entry | نظام القيد المزدوج | - |

### 9.3 الموافقات المطلوبة

- [ ] موافقة على الخطة العامة
- [ ] موافقة على تصميم قاعدة البيانات
- [ ] موافقة على دليل الحسابات
- [ ] موافقة على الجدول الزمني
- [ ] موافقة على البدء بالتنفيذ

---

## 📝 ملاحظات ختامية

هذه الخطة تمثل رؤية شاملة لتحويل نظام ياسمين الشام إلى نظام محاسبي متكامل بمستوى عالمي. تم تصميمها مع مراعاة:

1. **المعايير الدولية**: الالتزام بـ IFRS/GAAP
2. **خصوصية العمل**: دعم الفروع الثلاثة (التفصيل، الجاهز، الأقمشة)
3. **سهولة الاستخدام**: واجهة عربية احترافية
4. **الأمان**: سجل تدقيق شامل وصلاحيات دقيقة
5. **المرونة**: قابلية التوسع والتخصيص
6. **الأداء**: تحسينات للسرعة والاستجابة

**⏳ الخطوة التالية:** مراجعة هذه الخطة والموافقة عليها للبدء بالتنفيذ.

---

*تم إعداد هذه الخطة بتاريخ: ديسمبر 2024*
*إصدار الوثيقة: 1.0*