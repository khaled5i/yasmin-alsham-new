# تطبيق Migration للترقيم التسلسلي البسيط للطلبات

## التاريخ: 2026-01-31

---

## 📋 الهدف
تحويل نظام ترقيم الطلبات من النظام المعقد (مثل: 2025-0001) إلى نظام ترقيم تسلسلي بسيط (1، 2، 3، ...)

---

## ✅ الملفات المعدلة

### 1. Migration File (موجود بالفعل)
**الملف:** `migrations/12-simple-order-number.sql`

هذا الملف يحتوي على:
- دالة `generate_order_number()` المحدثة لتوليد أرقام تسلسلية بسيطة
- الدالة تبحث عن أكبر رقم طلب رقمي موجود وتضيف 1
- تتعامل مع الأرقام القديمة (غير الرقمية) بتجاهلها

### 2. واجهة المستخدم
**الملف:** `src/app/dashboard/add-order/page.tsx`

التعديلات:
- ✅ تغيير label من "(اختياري)" إلى "(تلقائي)"
- ✅ تغيير placeholder إلى "سيتم التوليد تلقائياً (1، 2، 3...)"
- ✅ إضافة نص توضيحي: "اتركه فارغاً للترقيم التلقائي التسلسلي"
- ✅ تغيير background إلى `bg-gray-50` للإشارة إلى أنه حقل تلقائي

---

## 🔧 خطوات التطبيق

### الخطوة 1: تطبيق Migration على قاعدة البيانات

قم بتشغيل الأمر التالي في Supabase SQL Editor أو من خلال terminal:

```bash
# إذا كنت تستخدم Supabase CLI
supabase db push

# أو قم بنسخ محتوى الملف وتشغيله مباشرة في SQL Editor
```

أو قم بنسخ ولصق محتوى الملف `migrations/12-simple-order-number.sql` في Supabase SQL Editor وتشغيله.

### الخطوة 2: التحقق من التطبيق

بعد تطبيق الـ migration، قم بإنشاء طلب جديد بدون إدخال رقم طلب يدوياً.
يجب أن يتم توليد رقم الطلب تلقائياً كرقم تسلسلي بسيط.

**مثال:**
- الطلب الأول: `1`
- الطلب الثاني: `2`
- الطلب الثالث: `3`
- وهكذا...

---

## 📝 ملاحظات مهمة

1. **الطلبات القديمة:**
   - الطلبات القديمة التي لها أرقام بصيغة `2025-0001` ستبقى كما هي
   - الدالة الجديدة تتجاهل الأرقام غير الرقمية عند حساب الرقم التالي

2. **الإدخال اليدوي:**
   - لا يزال بإمكان المستخدم إدخال رقم طلب يدوياً إذا أراد
   - إذا ترك الحقل فارغاً، سيتم التوليد التلقائي

3. **التسلسل:**
   - الترقيم يبدأ من 1 ويزداد بمقدار 1 مع كل طلب جديد
   - الدالة تبحث عن أكبر رقم رقمي موجود في قاعدة البيانات

---

## 🧪 اختبار النظام

### اختبار 1: إنشاء طلب بدون رقم
1. افتح صفحة إضافة طلب جديد
2. اترك حقل "رقم الطلب" فارغاً
3. أكمل باقي الحقول واحفظ الطلب
4. تحقق من أن رقم الطلب تم توليده تلقائياً كرقم تسلسلي

### اختبار 2: إنشاء عدة طلبات
1. أنشئ 3 طلبات متتالية بدون إدخال رقم يدوياً
2. تحقق من أن الأرقام متسلسلة: 1، 2، 3 (أو تبدأ من آخر رقم موجود + 1)

### اختبار 3: الإدخال اليدوي
1. أدخل رقم طلب يدوياً (مثل: "100")
2. احفظ الطلب
3. أنشئ طلب جديد بدون إدخال رقم
4. تحقق من أن الرقم الجديد هو 101

---

## ✅ النتيجة المتوقعة

بعد تطبيق هذا الـ migration:
- ✅ أرقام الطلبات الجديدة ستكون تسلسلية بسيطة (1، 2، 3...)
- ✅ واجهة المستخدم توضح أن الترقيم تلقائي
- ✅ المستخدم يمكنه إدخال رقم يدوياً إذا أراد
- ✅ النظام يحافظ على التسلسل حتى مع الإدخال اليدوي

