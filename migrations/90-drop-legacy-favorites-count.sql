-- ========================================
-- Migration 90: حذف آخر أثر لميزة المفضلة/السلة القديمة
-- ========================================
-- التاريخ: 2026-09-19
-- السياق: ميزة المفضلة والسلة القديمة (جداول favorites و cart_items) أُلغيت
--         وحُذفت جداولها، ودوالها حُذفت في 24-cleanup-orphaned-functions.sql.
--         بقي عمود عدّاد واحد على جدول الأقمشة لا يقرأه ولا يكتبه أي كود.
--
-- الأثر: العمود كان يساوي 0 في كل صفوف fabrics الـ449 وقت التطبيق،
--        فلا توجد بيانات تُفقد. ملفا الترحيل المؤرشفان
--        06a-favorites-cart-migration.sql و 06a-README-favorites-cart.md
--        حُذفا من المستودع مع هذا الترحيل.
--
-- ⚠️ غير قابل للتراجع: إعادة إنشاء العمود تعيد أصفاراً فقط.
-- ========================================

ALTER TABLE public.fabrics DROP COLUMN IF EXISTS favorites_count;
