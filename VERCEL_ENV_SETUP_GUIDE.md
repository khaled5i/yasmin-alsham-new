# دليل إعداد متغيرات البيئة على Vercel
# Vercel Environment Variables Setup Guide

## 🚨 المشكلة

عند نشر الموقع على Vercel، ظهر الخطأ التالي:
```
Error: supabaseUrl is required.
```

**السبب:** متغيرات البيئة الخاصة بـ Supabase غير موجودة على Vercel.

---

## ✅ الحل: إضافة متغيرات البيئة على Vercel

### الخطوة 1: افتح إعدادات المشروع

1. اذهب إلى: https://vercel.com/dashboard
2. اختر مشروعك: **yasmin-alsham-new**
3. اضغط على **Settings** (الإعدادات)
4. من القائمة الجانبية، اختر **Environment Variables**

---

### الخطوة 2: أضف المتغيرات التالية

انسخ والصق هذه المتغيرات **واحدة تلو الأخرى**:

#### 1️⃣ NEXT_PUBLIC_SUPABASE_URL

- **Name:** `NEXT_PUBLIC_SUPABASE_URL`
- **Value:** `https://qbbijtyrikhybgszzbjz.supabase.co`
- **Environment:** اختر الثلاثة (Production, Preview, Development)
- اضغط **Add**

#### 2️⃣ NEXT_PUBLIC_SUPABASE_ANON_KEY

- **Name:** `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- **Value:** 
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFiYmlqdHlyaWtoeWJnc3p6Ymp6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjE4MzQ5OTUsImV4cCI6MjA3NzQxMDk5NX0.8frVX_2mIRlVt_ofKcjEZRn3por7_x8j2Bhlu6_W87Q
```
- **Environment:** اختر الثلاثة (Production, Preview, Development)
- اضغط **Add**

#### 3️⃣ SUPABASE_SERVICE_ROLE_KEY

- **Name:** `SUPABASE_SERVICE_ROLE_KEY`
- **Value:**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFiYmlqdHlyaWtoeWJnc3p6Ymp6Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2MTgzNDk5NSwiZXhwIjoyMDc3NDEwOTk1fQ.AfcPCxZDOJAb6sFkmFocXBmi0icwiOPU0JP4wK4bDFM
```
- **Environment:** اختر الثلاثة (Production, Preview, Development)
- اضغط **Add**

#### 4️⃣ OPENAI_API_KEY (اختياري - لميزة تحويل الصوت إلى نص)

- **Name:** `OPENAI_API_KEY`
- **Value:** `[انسخ القيمة من ملف .env.local المحلي]`
- **Environment:** اختر الثلاثة (Production, Preview, Development)
- اضغط **Add**

**ملاحظة:** احصل على قيمة OPENAI_API_KEY من ملف `.env.local` في مشروعك المحلي، أو من https://platform.openai.com/api-keys

---

### الخطوة 3: إعادة النشر (Redeploy)

بعد إضافة جميع المتغيرات:

1. ارجع إلى صفحة **Deployments**
2. اضغط على النقاط الثلاث `...` بجانب آخر deployment
3. اختر **Redeploy**
4. اضغط **Redeploy** مرة أخرى للتأكيد

---

## 🎯 التحقق من نجاح العملية

بعد إعادة النشر، يجب أن:

1. ✅ يكتمل البناء (Build) بنجاح بدون أخطاء
2. ✅ يعمل الموقع بشكل طبيعي
3. ✅ تظهر البيانات من Supabase بشكل صحيح
4. ✅ يمكن إضافة طلبات جديدة بدون مشاكل

---

## 🔍 التحقق من المتغيرات

للتأكد من أن المتغيرات تم إضافتها بشكل صحيح:

1. اذهب إلى **Settings** > **Environment Variables**
2. يجب أن ترى 4 متغيرات:
   - ✅ `NEXT_PUBLIC_SUPABASE_URL`
   - ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - ✅ `SUPABASE_SERVICE_ROLE_KEY`
   - ✅ `OPENAI_API_KEY`

---

## 🐛 حل المشاكل

### المشكلة: لا تزال الأخطاء موجودة بعد إضافة المتغيرات

**الحل:**
1. تأكد من أنك أضفت المتغيرات للبيئات الثلاث (Production, Preview, Development)
2. تأكد من عدم وجود مسافات زائدة في القيم
3. قم بإعادة النشر (Redeploy) مرة أخرى

### المشكلة: "supabaseUrl is required" لا يزال يظهر

**الحل:**
1. تحقق من أن اسم المتغير صحيح تماماً: `NEXT_PUBLIC_SUPABASE_URL`
2. تأكد من أن القيمة تبدأ بـ `https://`
3. احذف المتغير وأضفه مرة أخرى

### المشكلة: البيانات لا تظهر على الموقع المنشور

**الحل:**
1. تحقق من أن قاعدة البيانات Supabase تحتوي على بيانات
2. تحقق من سياسات RLS في Supabase
3. افتح Console في المتصفح وابحث عن أخطاء

---

## 📝 ملاحظات مهمة

1. **الأمان:** 
   - لا تشارك `SUPABASE_SERVICE_ROLE_KEY` مع أحد
   - لا تضعه في الكود أو في GitHub
   - استخدمه فقط في متغيرات البيئة

2. **OpenAI API Key:**
   - إذا لم تكن تستخدم ميزة تحويل الصوت إلى نص، يمكنك تجاهل هذا المتغير
   - تأكد من أن لديك رصيد كافٍ في حساب OpenAI

3. **التحديثات المستقبلية:**
   - إذا قمت بتغيير مشروع Supabase، يجب تحديث هذه المتغيرات
   - إذا قمت بتجديد API Keys، يجب تحديثها على Vercel أيضاً

---

## 🎉 تم بنجاح!

بعد اتباع هذه الخطوات، يجب أن يعمل موقعك على Vercel بشكل طبيعي! 🚀

---

**تاريخ الإنشاء:** 2026-01-29  
**الحالة:** ✅ جاهز للتطبيق

