# دليل ترحيل نظام الطلبات من localStorage إلى Supabase

## 📋 نظرة عامة

تم ترحيل نظام الطلبات بالكامل من localStorage إلى Supabase لتحسين الأداء والموثوقية ودعم المستخدمين المتعددين.

## ✅ التغييرات المنفذة

### 1. قاعدة البيانات (Database)

#### ملف الترحيل: `migrations/03-orders-migration.sql`

**الجداول المنشأة:**
- `orders` - جدول الطلبات الرئيسي

**الميزات:**
- ✅ رقم طلب فريد تلقائي بصيغة `YYYY-NNNN`
- ✅ دعم العملاء المسجلين وغير المسجلين
- ✅ ربط بالعمال (workers)
- ✅ مقاسات مرنة باستخدام JSONB
- ✅ تتبع الدفعات (unpaid, partial, paid)
- ✅ حالات الطلب (pending, in_progress, completed, delivered, cancelled)
- ✅ صور متعددة وملاحظات صوتية
- ✅ Indexes للأداء الأمثل

**سياسات RLS (Row Level Security):**
- ✅ Admin فقط يمكنه إنشاء الطلبات (INSERT)
- ✅ Admin يمكنه عرض/تعديل/حذف جميع الطلبات
- ✅ Workers يمكنهم عرض/تعديل الطلبات المخصصة لهم
- ✅ العملاء يمكنهم عرض طلباتهم فقط

**الدوال المساعدة:**
- `generate_order_number()` - توليد رقم طلب فريد
- `get_orders_by_phone()` - البحث بواسطة رقم الهاتف
- `get_order_by_number()` - البحث بواسطة رقم الطلب

### 2. الخدمات (Services)

#### ملف: `src/lib/services/order-service.ts`

**العمليات المتاحة:**
- `create()` - إنشاء طلب جديد (Admin فقط)
- `getAll()` - جلب جميع الطلبات مع فلاتر اختيارية
- `getById()` - جلب طلب واحد بواسطة ID
- `getByOrderNumber()` - جلب طلب بواسطة رقم الطلب
- `getByPhone()` - جلب طلبات العميل بواسطة رقم الهاتف
- `update()` - تحديث طلب
- `delete()` - حذف طلب (Admin فقط)

**الأنواع (Types):**
```typescript
interface Order {
  id: string
  order_number: string
  user_id?: string | null
  worker_id?: string | null
  client_name: string
  client_phone: string
  client_email?: string | null
  description: string
  fabric?: string | null
  measurements: Record<string, any>
  price: number
  paid_amount: number
  payment_status: 'unpaid' | 'partial' | 'paid'
  status: 'pending' | 'in_progress' | 'completed' | 'delivered' | 'cancelled'
  due_date: string
  delivery_date?: string | null
  notes?: string | null
  admin_notes?: string | null
  images?: string[]
  voice_notes?: string[]
  completed_images?: string[]
  created_at: string
  updated_at: string
}
```

### 3. المخزن (Store)

#### ملف: `src/store/orderStore.ts`

**الحالة (State):**
- `orders` - قائمة الطلبات
- `currentOrder` - الطلب الحالي
- `isLoading` - حالة التحميل
- `error` - رسالة الخطأ

**العمليات:**
- `loadOrders()` - تحميل الطلبات مع فلاتر
- `loadOrderById()` - تحميل طلب واحد
- `loadOrderByNumber()` - تحميل طلب بواسطة رقم الطلب
- `loadOrdersByPhone()` - تحميل طلبات بواسطة رقم الهاتف
- `createOrder()` - إنشاء طلب جديد
- `updateOrder()` - تحديث طلب
- `deleteOrder()` - حذف طلب
- `startOrderWork()` - بدء العمل في الطلب
- `completeOrder()` - إنهاء الطلب
- `getStats()` - الحصول على إحصائيات الطلبات (totalOrders, completedOrders, activeOrders, totalRevenue, etc.)

### 4. الصفحات المحدثة

#### `src/app/dashboard/add-order/page.tsx`
- ✅ استخدام `useOrderStore` بدلاً من `useDataStore`
- ✅ استخدام `useWorkerStore` لجلب العمال
- ✅ تحويل البيانات إلى صيغة Supabase
- ✅ معالجة الأخطاء المحسنة

#### `src/app/dashboard/orders/page.tsx`
- ✅ استخدام `useOrderStore` و `useWorkerStore`
- ✅ تحميل البيانات عند تحميل الصفحة
- ✅ تحديث دوال معالجة الطلبات
- ✅ تحويل البيانات بين الصيغ القديمة والجديدة

#### `src/app/track-order/page.tsx`
- ✅ استخدام `loadOrderByNumber()` و `loadOrdersByPhone()`
- ✅ البحث المباشر في Supabase
- ✅ عرض النتائج بشكل صحيح

#### `src/app/dashboard/page.tsx`
- ✅ استخدام `useOrderStore` و `useAppointmentStore`
- ✅ تحميل البيانات عند تحميل الصفحة
- ✅ تحديث أسماء الحقول (client_name, worker_id, created_at, due_date)

#### `src/app/dashboard/reports/page.tsx`
- ✅ استخدام `useOrderStore`, `useWorkerStore`, `useAppointmentStore`
- ✅ تحديث حسابات الإحصائيات
- ✅ تحديث أسماء الحقول

### 5. المكونات المحدثة

#### `src/components/OrderModal.tsx`
- ✅ استخدام أنواع Order و Worker من الخدمات
- ✅ تحديث أسماء الحقول (client_name, worker_id, created_at, due_date, voice_notes)
- ✅ تحويل voice_notes من string[] إلى الصيغة المطلوبة

#### `src/components/EditOrderModal.tsx`
- ✅ استخدام أنواع Order و Worker من الخدمات
- ✅ تحديث useEffect لتحويل البيانات
- ✅ تحويل voice_notes بشكل صحيح

## 🔄 خطوات التطبيق

### 1. تطبيق Migration على Supabase

```bash
# تشغيل migration في Supabase SQL Editor
# نسخ محتوى migrations/03-orders-migration.sql
# لصقه في SQL Editor وتشغيله
```

### 2. التحقق من RLS Policies

```sql
-- التحقق من أن السياسات تم إنشاؤها بنجاح
SELECT * FROM pg_policies WHERE tablename = 'orders';
```

### 3. اختبار النظام

#### اختبار إنشاء طلب (Admin):
1. تسجيل الدخول كـ Admin
2. الذهاب إلى "إضافة طلب جديد"
3. ملء النموذج وإرساله
4. التحقق من ظهور الطلب في قائمة الطلبات

#### اختبار عرض الطلبات (Worker):
1. تسجيل الدخول كـ Worker
2. الذهاب إلى "الطلبات"
3. التحقق من ظهور الطلبات المخصصة فقط

#### اختبار تتبع الطلب (Public):
1. الذهاب إلى صفحة تتبع الطلب
2. البحث برقم الطلب أو رقم الهاتف
3. التحقق من ظهور النتائج

## 📊 الفروقات الرئيسية

### أسماء الحقول

| localStorage | Supabase |
|-------------|----------|
| `clientName` | `client_name` |
| `clientPhone` | `client_phone` |
| `assignedWorker` | `worker_id` |
| `dueDate` | `due_date` |
| `createdAt` | `created_at` |
| `updatedAt` | `updated_at` |
| `voiceNotes` | `voice_notes` |

### أنواع البيانات

| الحقل | localStorage | Supabase |
|------|-------------|----------|
| `voiceNotes` | `Array<{id, data, timestamp}>` | `string[]` (data only) |
| `measurements` | `Object` | `JSONB` |
| `price` | `number` | `DECIMAL(10, 2)` |

## ⚠️ ملاحظات مهمة

1. **Admin فقط يمكنه إنشاء الطلبات** - على عكس المواعيد التي تسمح بالحجز المجهول
2. **رقم الطلب يُنشأ تلقائياً** - لا حاجة لتوليده يدوياً
3. **الملاحظات الصوتية** - يتم تخزين البيانات فقط (data) في Supabase
4. **المقاسات** - مخزنة كـ JSONB للمرونة
5. **الصور** - مخزنة كـ TEXT[] (URLs أو base64)

## 🐛 استكشاف الأخطاء

### خطأ: "Supabase is not configured"
- تأكد من وجود متغيرات البيئة في `.env.local`
- تحقق من صحة SUPABASE_URL و SUPABASE_ANON_KEY

### خطأ: "new row violates row-level security policy"
- تأكد من أن المستخدم لديه الصلاحيات المناسبة
- تحقق من RLS policies في Supabase

### خطأ: "Worker not found"
- تأكد من تحميل العمال قبل عرض الطلبات
- استخدم `loadWorkers()` في useEffect

### خطأ: "Cannot read properties of undefined (reading 'toLowerCase')"
- ✅ **تم الإصلاح:** تحديث جميع أسماء الحقول من camelCase إلى snake_case
- تأكد من استخدام `order.client_name` بدلاً من `order.clientName`
- تأكد من استخدام `order.worker_id` بدلاً من `order.assignedWorker`
- تأكد من استخدام `order.due_date` بدلاً من `order.dueDate`
- تأكد من استخدام `order.created_at` بدلاً من `order.createdAt`
- أضف فحص للقيم: `(order.client_name || '').toLowerCase()`

### خطأ: "Supabase error updating order: {}"
- ✅ **تم الإصلاح:** تحسين معالجة الأخطاء في `order-service.ts`
- الآن يتم عرض تفاصيل الخطأ الكاملة: `message`, `details`, `hint`, `code`
- استخدام `error.message || error.hint` لعرض رسالة خطأ واضحة
- تسجيل جميع تفاصيل الخطأ في Console للمساعدة في التشخيص

### خطأ: "كلمة المرور غير صحيحة" عند حذف الطلب
- ✅ **تم الإصلاح:** تحديث `DeleteOrderModal.tsx` لاستخدام Supabase Auth
- كان الكود يتحقق من كلمة مرور ثابتة `'admin123'` بدلاً من استخدام Supabase Auth
- الآن يستخدم `supabase.auth.signInWithPassword()` للتحقق من كلمة المرور الفعلية
- يدعم Fallback إلى localStorage للتطوير المحلي
- التحقق الآمن من كلمة المرور عبر Supabase Auth API

### خطأ: عرض مهنة العامل بدلاً من اسمه + الطلبات لا تظهر للعامل
- ✅ **تم الإصلاح:** مشكلتان في نظام إدارة الطلبات

#### المشكلة الأولى: عرض `specialty` بدلاً من `full_name` في قائمة العمال
- **السبب:** في `add-order/page.tsx` و `EditOrderModal.tsx`، كان الكود يستخدم `worker.full_name` مباشرة
- **الحل:** تحديث لاستخدام `worker.user?.full_name` (البيانات تأتي من علاقة `workers → users`)
- **الملفات المعدلة:**
  - `src/app/dashboard/add-order/page.tsx` (السطر 347)
  - `src/components/EditOrderModal.tsx` (السطر 309)

#### المشكلة الثانية: الطلبات المُعيّنة للعامل لا تظهر في صفحته
- **السبب:** كان الكود يقارن `order.worker_id === user.id` وهذا خطأ لأن:
  - `order.worker_id` → يشير إلى `workers.id`
  - `user.id` → يشير إلى `users.id`
  - العلاقة الصحيحة: `workers.user_id === users.id`
- **الحل:** البحث عن العامل أولاً ثم المقارنة:
  ```typescript
  const currentWorker = workers.find(w => w.user_id === user.id)
  if (currentWorker) {
    matchesRole = order.worker_id === currentWorker.id
  }
  ```
- **الملفات المعدلة:**
  - `src/app/dashboard/orders/page.tsx` (فلترة الطلبات + الإحصائيات)
  - `src/app/dashboard/page.tsx` (إحصائيات العامل + أحدث الطلبات)

#### التأثير:
- ✅ الآن يظهر اسم العامل الكامل في قائمة اختيار العامل
- ✅ العامل يرى طلباته المُعيّنة له بشكل صحيح
- ✅ الإحصائيات تعرض البيانات الصحيحة للعامل
- ✅ أحدث الطلبات تُفلتر بشكل صحيح حسب دور المستخدم

### خطأ: قائمة العمال فارغة في نافذة تعديل الطلب
- ✅ **تم الإصلاح:** تحديث نوع البيانات وإضافة تسجيل للتحقق من البيانات
- **السبب:** كان النوع المستخدم `Worker[]` بدلاً من `WorkerWithUser[]`
- **الحل:**
  1. تحديث الاستيراد والنوع:
     ```typescript
     // ❌ قبل
     import { Worker } from '@/lib/services/worker-service'
     workers: Worker[]

     // ✅ بعد
     import { WorkerWithUser } from '@/lib/services/worker-service'
     workers: WorkerWithUser[]
     ```
  2. إضافة `console.log` للتحقق من البيانات عند فتح النافذة
- **الملف المعدل:** `src/components/EditOrderModal.tsx`
- **التأثير:** الآن تظهر قائمة العمال بشكل صحيح في نافذة تعديل الطلب
- **للتحقق:** افتح Console في المتصفح عند فتح نافذة التعديل لرؤية تفاصيل قائمة العمال

### خطأ: نصوص باللغة الإنجليزية في صفحة إضافة طلب
- ✅ **تم الإصلاح:** إضافة مفاتيح الترجمة المفقودة
- **السبب:** مفاتيح الترجمة التالية كانت مفقودة من ملف `useTranslation.ts`:
  - `add_new_order_description`
  - `basic_information`
  - `basic_measurements`
  - `advanced_measurements`
  - `sleeve_measurements`
  - `length_measurements`
- **الحل:** إضافة جميع المفاتيح المفقودة إلى كل من `arTranslations` و `enTranslations`
- **الملف المعدل:** `src/hooks/useTranslation.ts`
- **التأثير:**
  - ✅ جميع النصوص في صفحة إضافة طلب تظهر بالعربية عند اختيار اللغة العربية
  - ✅ جميع النصوص تظهر بالإنجليزية عند اختيار اللغة الإنجليزية
  - ✅ لا توجد مفاتيح إنجليزية ظاهرة كنص بدلاً من الترجمة

## 📝 TODO (اختياري)

- [ ] إضافة pagination للطلبات
- [ ] إضافة real-time updates باستخدام Supabase subscriptions
- [ ] تحسين البحث باستخدام Full-Text Search
- [ ] إضافة تصدير التقارير PDF/Excel
- [ ] إضافة إشعارات للعملاء عند تغيير حالة الطلب

## ✨ الخلاصة

تم ترحيل نظام الطلبات بنجاح من localStorage إلى Supabase مع:
- ✅ جميع الوظائف الأساسية تعمل
- ✅ RLS policies محكمة
- ✅ أداء محسّن مع Indexes
- ✅ دعم المستخدمين المتعددين
- ✅ معالجة أخطاء شاملة

